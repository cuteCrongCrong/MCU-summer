# 📖 MCU 학습 플랫폼 사용 가이드

**누구를 위한 문서인가** — 이 사이트를 쓰는 사람(의대생) 전용입니다.
코드를 올리거나 서버를 다루는 방법은 [팀원_가이드.md](팀원_가이드.md) · [서버_가이드.md](서버_가이드.md) 를 보세요.

---

## 목차

1. [30초 요약](#30초-요약)
2. [시작하기 — 로그인과 API 키](#1-시작하기--로그인과-api-키)
3. [📝 문제 생성](#2--문제-생성)
4. [🔍 기출 주제 분석](#3--기출-주제-분석)
5. [🗂️ 보관함](#4-️-보관함)
6. [❌ 오답 노트](#5--오답-노트)
7. [🦴 골학 문제은행](#6--골학-문제은행)
8. [🖨 인쇄 · PDF 저장](#7--인쇄--pdf-저장)
9. [💰 값(토큰) 아끼는 법](#8--값토큰-아끼는-법)
10. [🚨 자주 겪는 문제](#9--자주-겪는-문제)
11. [🔒 내 데이터는 어떻게 다뤄지나](#10--내-데이터는-어떻게-다뤄지나)

---

## ✅ 안심해도 되는 것들 (먼저 읽으면 마음이 편합니다)

처음 쓰는 분들이 가장 많이 걱정하는 것들부터 답해 둡니다. 자세한 내용은 [10장](#10--내-데이터는-어떻게-다뤄지나)에 있습니다.

- **올린 PDF가 서버에 쌓이지 않습니다.** 분석하는 동안만 임시로 두고, 끝나는 즉시 지웁니다. 오류나 중지로 삭제를 못 밟았더라도 **최대 1시간 안에 자동으로 치웁니다.** 남는 것은 파일이 아니라 분석 *결과*(세션)뿐입니다.
- **API 키는 서버에도 브라우저에도 저장되지 않습니다.** 요청에 쓰고 바로 버립니다. 그래서 새로고침하면 다시 입력해야 합니다 — 불편함이 곧 안전장치입니다.
- **다른 사람이 내 기록을 볼 수 없습니다.** 세션·시험지·주제·오답은 모두 내 계정(또는 내 브라우저)에만 묶여 있습니다.
- **생성 중에 다른 탭으로 옮겨 다녀도 됩니다.** 골학을 풀거나 보관함을 뒤져도 문제는 그대로 만들어집니다. (탭을 *닫거나* 새로고침하는 것만 피하면 됩니다)
- **만든 것은 자동으로 저장됩니다.** 문제 세트도, 주제 분석 결과도 저장 버튼을 누를 필요가 없습니다. 실수로 뒤로 가도 보관함에 그대로 있습니다.
- **잘못 눌러 값이 새어 나가지 않게 막아 뒀습니다.** 값이 드는 확인 모달은 바깥을 눌러도 닫히지 않고, 같은 자료를 다시 분석하려 하면 「보관된 결과 열기(0원)」를 먼저 권합니다.
- **골학 문제은행은 완전히 공짜입니다.** API 키도, 로그인도, 인터넷 요금도 신경 쓸 것 없습니다.
- **문제지를 인쇄해도 정답이 딸려 나가지 않습니다.** 화면에서 정답을 열어 뒀더라도 문제지에는 애초에 정답 글자가 들어가지 않습니다.
- **틀려도 기록이 남아 창피할 일이 없습니다.** 골학 점수는 그 자리에서만 세고 서버로 보내지 않습니다. 오답 노트는 *내가 담기로 누른 것만* 저장됩니다.

---

## 30초 요약

| 하고 싶은 것 | 들어갈 곳 | API 키 | 값 |
|---|---|---|---|
| 강의자료 + 기출로 **예상문제 만들기** | 📝 문제 생성 | 필요 | 든다 |
| 강의록 중 **기출에 나온 부분만 찾기** | 🔍 기출 주제 분석 | 필요 | 든다 |
| 만들어 둔 문제·분석 **다시 보기 / 인쇄** | 🗂️ 보관함 | 불필요 | 0 |
| 틀린 문제 **모아 보기** | ❌ 오답 노트 | 불필요 | 0 |
| **뼈 이름 외우기** (자료 없이 바로) | 🦴 골학 문제은행 | 불필요 | 0 |

> 💡 **깨알** — 홈 화면의 카드는 성격별로 세 묶음입니다. **만들기**(내 자료를 넣는 것) · **복습하기**(이미 만든 것) · **바로 풀기**(아무 준비 없이 되는 것). 뭘 눌러야 할지 모르겠으면 이 분류부터 보세요.

---

## 1. 시작하기 — 로그인과 API 키

### 로그인 (선택이지만 권장)

로그인 없이도 **모든 기능이 동작합니다.** 다만 기록이 남는 위치가 다릅니다.

| | 로그인함 | 로그인 안 함(게스트) |
|---|---|---|
| 세션·보관함·오답 노트 | 계정에 저장 | **이 브라우저에만** 임시 저장 |
| 다른 기기/브라우저에서 보기 | ✅ | ❌ |
| 시크릿 모드·쿠키 삭제 후 | 그대로 | **사라짐** |

> ⚠️ **가장 중요한 주의사항** — 게스트로 만든 기록은 **나중에 로그인해도 계정으로 옮겨지지 않습니다.** 남겨 둘 생각이면 *시작하기 전에* 로그인하세요. (첫 방문 때 뜨는 안내 모달이 이 얘기입니다. 한 번 닫으면 그 브라우저에서는 다시 안 뜹니다.)

### API 키

문제 생성과 기출 주제 분석은 LLM을 부르므로 키가 필요합니다. 제공사를 고르면 그에 맞는 발급 안내가 입력칸 아래에 펼쳐집니다.

- **전북대 LLM 플랫폼(게이트웨이)** — 기본값. 이 제공사만 **크레딧 잔액 조회**를 지원합니다(키 입력칸 아래 `↻ 조회`).
- OpenAI · Anthropic · Gemini — 개인 키를 직접 넣어 쓸 수 있습니다.

> 🔒 입력한 키는 **서버에도 브라우저에도 저장되지 않습니다.** 요청에 쓰고 버립니다.
> 💡 **깨알** — 저장하지 않는 대신, 페이지를 켜 둔 동안에는 **제공사별로 따로** 기억합니다. 제공사를 바꿨다가 되돌아와도 키를 다시 칠 필요는 없습니다. 대신 **새로고침하면 다시 입력**해야 합니다.
> 💡 **깨알** — 키를 넣으면 모델 목록이 자동으로 채워집니다. 안 채워지면 `↻` 버튼으로 다시 부르세요. 목록이 안 뜨는 건 대개 키가 틀렸다는 뜻입니다.

---

## 2. 📝 문제 생성

강의자료 PDF와 기출문제 PDF를 넣으면 **기출의 유형·형식을 반영한 새 예상문제**를 만듭니다.

### 준비물과 업로드 규칙

| 항목 | 제한 |
|---|---|
| 형식 | **PDF만** |
| 개수 | 강의자료 최대 **7개**, 기출 최대 **7개** (각각) |
| 용량 | 강의자료 + 기출 **전부 합쳐서 500MB** (서버 설정에 따라 다를 수 있음 — 화면에 표시된 숫자가 정답) |
| 문제 수 | 1 ~ **30개** |

- 강의자료·기출 **둘 다** 있어야 시작할 수 있습니다(저장된 세션을 쓸 때는 예외).
- 파일은 끌어다 놓거나 클릭해서 고릅니다.

> ✅ **안심** — 올린 PDF는 **서버에 쌓이지 않습니다.** 읽는 동안만 임시 사본을 두고 끝나면 바로 지웁니다. 남는 건 분석 결과뿐이라, 강의자료를 통째로 올려도 어딘가에 파일이 남아 돌아다닐 일이 없습니다.
> 💡 **깨알** — 용량 초과는 **업로드하기 전에** 화면에서 먼저 막습니다. 500MB를 다 올려놓고 거부당하는 헛수고가 없습니다.
> 💡 **깨알** — 파일 수를 줄이면 **파일당 글자 배정량이 늘어나** 덜 잘립니다. 300쪽짜리 하나가 30쪽짜리 7개보다 더 깊게 읽힐 수 있습니다.

### 설정 항목

**① 유형별 문제 수 — 자동 / 수동**

- **자동(기본)**: 총 개수만 정하면, **기출의 유형 비율 그대로** 객관식/빈칸채우기/단답형/서술형에 배분합니다.
- **`기출에 있는 유형은 최소 1개씩 넣기`**: 비중이 작은 유형(예: 서술형 1문항)이 비율 계산에서 **0개로 반올림돼 사라지는 것**을 막습니다. 대신 기출 비율과는 조금 어긋납니다.
- **수동**: 유형별 개수를 직접 씁니다. 합계가 1~30 안이어야 합니다.

> 💡 **깨알** — 수동으로 바꾸면 `최소 1개씩 넣기` 옵션이 화면에서 사라집니다. 자동 배분에만 의미가 있는 설정이라 일부러 숨깁니다.

**② 기출 반영 강도 (1~10)**

| 값 | 성격 |
|---|---|
| 1~3 | 🌱 기출은 참고만. 강의자료 핵심 개념 위주로 자유롭게 출제, 형식도 느슨하게 |
| 4~7 | ⚖️ 균형 — 기출 경향·형식을 반영하되 강의자료 개념도 폭넓게 |
| 8~10 | 🔒 기출 밀착 — 개념·문투·형식을 거의 그대로 재현, 기출과 겹치는 주제 위주 |

> 시험 직전 실전 감각용이면 8~10, 범위를 넓게 훑고 싶으면 3~5를 권합니다.

**③ 세트 이름 (선택)** — 비워두면 `제N회`로 저장됩니다. 나중에 보관함에서 ✏️로 바꿀 수 있습니다.

**④ 모델** — 문제를 쓰는 모델만 고릅니다. 그림 읽기·개념 분석에 쓰는 모델은 **서버가 제공사별로 알아서** 정합니다(고를 필요 없음).

### 생성 중에 일어나는 일

진행 상태는 4단계로 표시됩니다.

```
📄 PDF 텍스트 추출  →  🧠 강의·기출 핵심 개념 분석  →  🔍 기출 예시 추출·형식 분석  →  ✏️ 예상문제 생성
```

- **다른 탭(골학·주제 분석 등)으로 옮겨 다녀도 생성은 계속됩니다.**
- **브라우저 탭을 닫거나 새로고침하면 중단됩니다.** 이것만 조심하세요.
- `■ 중지`로 언제든 멈출 수 있습니다. 단, **그때까지 쓴 값은 이미 나간 값**이라 돌아오지 않습니다.
- 문제는 다 끝난 뒤 한꺼번에가 아니라 **만들어지는 대로 하나씩** 화면에 나타납니다.

> 💡 **깨알** — 기출의 **그림 페이지는 Vision 모델이 읽어** 설명을 남긴 뒤 문제 재료로 씁니다. 그래서 그림 문제(해부도·그래프)가 있는 기출도 반영됩니다. 대신 그림이 많을수록 시간과 값이 늘어납니다.
> 💡 **깨알** — 한글 폰트가 깨진 PDF(글자가 `C6넍carotid…` 처럼 밀려 나오는 것)는 **자동으로 되살립니다.** 되살리기가 애매하면 지어내지 않고 그 페이지를 **이미지로 읽습니다.**

**⚠️ 「일부 파일이 분량 상한을 넘습니다」 모달이 뜨면**

자료가 배정된 글자 예산보다 길어서, 일부가 잘린 채로 진행된다는 뜻입니다.

- **이대로 진행** — 잘린 상태로 계속합니다. (앞부분 70% + 뒷부분 30%를 남깁니다)
- **취소** — 멈춥니다. 단, **PDF 추출까지 이미 쓴 값은 돌아오지 않습니다** (모달에 그 액수가 표시됩니다).
- 다시 할 거라면 **파일 수를 줄여** 파일당 배정량을 늘리는 쪽이 낫습니다.

> 💡 **깨알** — 이 모달은 **바깥을 눌러도 닫히지 않습니다.** 실수로 배경을 눌러 이미 값을 치른 추출 결과가 날아가는 일을 막기 위해서입니다. 두 버튼 중 하나로만 답할 수 있습니다.
> 💡 **깨알** — 「이대로 진행」을 눌러도 PDF를 **다시 읽지 않습니다.** 이미 뽑아 둔 텍스트로 이어 돌기 때문에 Vision 값이 두 번 나가지 않습니다.

### 결과 화면에서 할 수 있는 것

- **객관식** — 선택지를 고르고 `정답 확인`. `모두 고르시오` 유형은 **여러 개를 켤 수 있습니다**(누를 때마다 켜짐/꺼짐).
- **빈칸채우기** — 빈칸이 2개 이상이면 **칸마다 따로 입력하고 칸별로 O/X 채점**합니다. 대소문자·공백은 무시합니다.
- **단답형·서술형** — 답을 써 본 뒤 `정답 확인`으로 모범답안과 해설을 엽니다.
- **🔖 오답에 넣기** — 문제 하나를 오답 노트 폴더에 담습니다.
- **사용량** — 맨 아래 접힌 상자에 이번 회차에 쓴 토큰·크레딧이 나옵니다.

> 💡 **깨알** — 생성한 문제 세트는 **자동으로 보관함에 저장**됩니다. 따로 저장 버튼을 누를 필요가 없습니다.
> 💡 **깨알** — 수식은 LaTeX 원문(`$\Delta G$`)이 아니라 **읽기 좋은 기호(ΔG)** 로 보입니다. 채점도 **화면에 보이는 대로** 비교하므로, 본 대로 `ΔG`를 쳐도 정답 처리됩니다.

### 💾 저장된 세션 — 같은 자료로 또 만들 때

PDF를 분석하면 그 결과(핵심 개념·기출 형식 분석)가 **세션으로 자동 저장**됩니다. 다음부터는 세션을 고르고 바로 생성하면 **분석 단계를 통째로 건너뜁니다.**

- 세션을 고르면 초록색 바에 `현재 세션: …`이 뜨고, 버튼 문구가 `이 세션으로 문제 생성 (분석 생략)`으로 바뀝니다.
- 진행 단계도 `✏️ 예상문제 생성` 하나만 남습니다.
- ✏️로 이름 변경, 🗑️로 삭제할 수 있습니다. **세션을 지우면 그 세션으로 만든 생성 이력도 함께 사라집니다.**

> 💡 **깨알** — 세션 목록은 최근 3개만 보이고, 나머지는 `이전 세션 N개 더 보기`로 펼칩니다. 선택한 세션이 3개 밖에 있으면 **그 세션은 항상 같이 보여줍니다.**
> 💡 **깨알** — 세션을 고른 상태에서 **새 파일을 올리면 세션이 자동으로 해제**됩니다(파일이 우선). 반대로 세션을 고르면 올려 둔 파일 선택이 비워집니다. 둘이 섞여 엉뚱하게 재분석되는 일을 막습니다.
> 💡 **깨알** — 홈 화면의 **`이어서 하기`** 카드는 가장 최근 세션입니다. 홈 → 버튼 한 번으로 바로 생성까지 갑니다.

---

## 3. 🔍 기출 주제 분석

문제를 만들지 않습니다. **강의록의 주제 중 기출에 실제로 나온 것만 골라내는** 기능입니다.

결과는 이런 모양입니다.

```
주제 이름
📄 강의록  [순환기1.pdf 12p 13p]  →  📝 기출  [2023중간.pdf 4번 7번]
📄 강의록 내용 — 해당 페이지에 적힌 내용 요약
"기출 원문 인용"
```

- 주제 이름은 **강의록에 적힌 표현 그대로** 씁니다. 강의록에 없는 용어는 만들어 쓰지 않습니다.
- 업로드 규칙(PDF·각 7개·합계 용량)은 문제 생성과 같습니다.
- 이 탭은 **기본 선택 모델이 따로** 잡혀 있습니다. 대조 작업 성격이 달라서입니다.

**화면 기능**

- `🔎 주제 · 파일명으로 걸러보기` — 주제명, 파일명, 출제형태, 발췌, 기출 원문까지 한 번에 검색합니다.
- `📋 한 줄 요약 보기 (복사용)` — `주제: 강의록 12p - 기출 4번` 형태의 평문 목록. **전체 복사** 버튼으로 통째로 복사해 노션·카톡에 붙일 수 있습니다.

> ⚠️ **스캔 이미지로만 된 PDF는 페이지·문항 번호를 읽을 수 없습니다.** 결과가 비었다면 (1) 같은 과목·같은 범위의 자료인지, (2) 스캔본이 아닌지 확인하세요.
> 💡 **깨알** — 출처(페이지·문항 번호)를 확인할 수 없는 항목은 **버립니다.** 목록 위 `제외한 항목 N개`가 그 숫자입니다. 근거 없는 주제를 채워 넣지 않겠다는 뜻입니다.
> 💡 **깨알** — **같은 자료를 다시 올리면** 「이미 분석한 결과가 있습니다」 모달이 뜹니다. `보관된 결과 열기`는 **값이 0원**입니다. 새로 분석하면 같은 값을 한 번 더 내고, 결과 문장도 매번 조금씩 달라집니다.
> 💡 **깨알** — 분석 결과도 **자동으로 보관함에 저장**됩니다.

---

## 4. 🗂️ 보관함

만들어 둔 것을 다시 보는 곳. 두 갈래입니다.

### 📄 생성한 문제

- 한 페이지에 **8장**씩, 최신순.
- 세션별로 걸러 볼 수 있습니다(위쪽 드롭다운).
- 카드에서 **✏️ 이름 변경**, **🗑️ 삭제**, **🖨 인쇄**.
- 한 장을 열면 그때 쓴 **토큰·크레딧 기록**도 함께 볼 수 있습니다.

### 🔍 분석한 주제

기출 주제 분석 결과 목록. 열면 분석 직후와 **똑같은 화면**(검색·한 줄 요약 포함)이 그대로 나옵니다.

> 💡 **깨알** — 목록에서 마지막 장의 카드를 지우면 **자동으로 이전 페이지로 당겨옵니다.** 빈 화면을 보게 되는 일이 없습니다.
> 💡 **깨알** — 오래된 기록에는 사용량 상자가 안 보일 수 있습니다. 기록을 남기기 전에 만든 회차라 값이 아예 없기 때문입니다. 0원이라는 뜻이 아닙니다.

---

## 5. ❌ 오답 노트

틀린 문제·헷갈린 문제를 **폴더로 모아 두는** 곳입니다.

- 어디서든 `🔖 오답에 넣기` → 기존 폴더를 고르거나 새 폴더를 만들어 담습니다.
- 담을 수 있는 것: **생성한 문제**, **골학 문제은행 문제**(그림째로).
- 폴더는 이름 변경·삭제, 문제는 `🗑️ 폴더에서 빼기`.
- 폴더를 열면 원래 문제와 똑같이 **다시 풀어볼 수 있습니다.**

> 💡 **깨알** — 골학 오답은 **번호 그림과 「원본(이름 표시) 그림」이 함께** 저장됩니다. 오답 노트에서도 `🖼️ 원본 보기`로 정답 라벨을 확인할 수 있습니다.

---

## 6. 🦴 골학 문제은행

**API 키도 자료도 필요 없고, 값이 들지 않습니다.** 순수하게 미리 넣어 둔 그림 데이터로 돌아갑니다.

### 범위 고르기

`Skull` · `Upper Limb` · `Lower Limb` · `Vertebrae & Thorax` (+ 전체 Random)
→ 카테고리 안에서 세부 범위(예: `Hand bones`, `Thorax`)까지 좁힐 수 있습니다.

### 출제 모드 3가지

| 모드 | 방식 | 언제 쓰나 |
|---|---|---|
| 🔤 **한 문제씩 풀기** | 번호 하나씩 랜덤 출제, 직접 타이핑 | 처음 외울 때 |
| 🖱️ **단어은행에서 골라 채우기** | 그림 한 장의 빈칸을 단어 카드로 매칭 | 이름이 가물가물할 때 |
| ✍️ **빈칸 한번에 쓰기** | 그림 한 장의 번호를 전부 입력 후 한 번에 채점 | 시험처럼 확인할 때 |

- 범위 안에서 **중복 없이** 한 번씩만 출제되고, 다 풀면 완료 화면(🏁)에서 멈춥니다. 끝없이 반복되지 않습니다.
- 상단에 `맞힘 / 푼 문제 / 정답률`이 계속 갱신됩니다. **이 점수는 화면에서만 세고 서버로 보내지 않습니다** — 마음 놓고 틀리세요.
- `🖼️ 원본 보기`로 이름이 적힌 원본 그림을 언제든 확인할 수 있습니다.
- 부위 이름 외에 **「이 구조물의 이름은?」 「어느 방향에서 본 모습인가?」** 문제가 섞여 나오기도 합니다.

### 채점 규칙 (알아두면 억울할 일이 없습니다)

| 입력 | 결과 |
|---|---|
| `Femur` / `femur` / `FEMUR` | ✅ 대소문자 무시 |
| `Supra-orbital notch` / `Supraorbital notch` / `supraorbitalnotch` | ✅ 공백·하이픈 무시 |
| 정답이 `Supra-orbital notch (foramen)` 일 때 → `Supra-orbital notch` | ✅ **괄호 부분은 생략 가능** |
| 빈칸 | ❌ 채점되지 않음 (한 문제씩 모드에서는 무시하고 대기) |

> 💡 **깨알** — **Enter 키만으로** 끝까지 갈 수 있습니다. 한 문제씩 모드에서는 채점 전 Enter = 확인, 채점 후 Enter = 다음 문제. 빈칸 쓰기 모드에서는 Enter = 다음 칸, 마지막 칸에서 Enter = 채점.
> 💡 **깨알** — 그림의 제목(예: `Right femur, anterior view`)은 **화면에 노출하지 않습니다.** 제목이 곧 정답 힌트가 되기 때문입니다.
> 💡 **깨알** — 「구조물 이름」·「방향」 문제는 번호가 필요 없으므로 **번호 없는 원본 그림**을 보여줍니다.
> 💡 **깨알** — 클릭형·빈칸쓰기 모드에서는 **그림과 단어은행이 화면 위에 고정**됩니다. 라벨이 30~40개인 그림에서도 스크롤을 오르내리지 않아도 됩니다.
> 💡 **깨알** — 모르겠으면 `🙈 모르겠어요`를 누르세요. 오답으로 기록되지만 정답을 바로 보여줍니다. 아무거나 찍는 것보다 정확한 통계가 남습니다.
> 💡 **깨알** — 탭을 떠났다 돌아와도 **점수는 유지**되지만 화면은 항상 범위 선택부터 다시 시작합니다.

---

## 7. 🖨 인쇄 · PDF 저장

보관함에서 시험지를 열면 **`🖨 문제지`** 와 **`🖨 정답·해설`** 두 버튼이 있습니다.

- **문제지** — 정답과 해설이 **문서에 아예 들어가지 않습니다.** 화면에서 `정답 확인`을 눌러 열어 뒀더라도 종이에는 찍히지 않습니다. 빈칸은 손글씨용 밑줄로 바뀝니다.
- **정답·해설지** — 위에 **빠른 정답표**(번호-정답 2열), 아래에 문항별 해설. 해설에는 문제 앞부분이 함께 실려 어느 문항인지 바로 알 수 있습니다.

> ⚠️ 두 벌을 한 번에 저장할 수는 없습니다. **버튼을 각각 눌러 두 번** 저장하세요.
> 💡 **깨알** — 인쇄 창에서 **「머리글 및 바닥글」을 끄면** URL·날짜가 빠져 깔끔합니다. 대신 **페이지 번호도 같이 사라집니다.**
> ⚠️ **카카오톡·인스타그램 안에서 링크를 열면 인쇄 창이 안 뜰 수 있습니다.** Safari나 Chrome으로 열어 주세요.
> 💡 **깨알** — 저장 파일명은 `세트이름_문제지_g12` 형태로 **제안**됩니다. 저장 창에서 얼마든지 바꿀 수 있습니다.
> 💡 **깨알** — 미리보기는 `Esc` 키로 닫힙니다.

---

## 8. 💰 값(토큰) 아끼는 법

값이 드는 건 **문제 생성**과 **기출 주제 분석** 둘뿐입니다. 순서대로 효과가 큽니다.

1. **저장된 세션을 쓰세요.** 같은 자료로 두 번째부터는 분석 단계(가장 비싼 구간)를 통째로 건너뜁니다.
2. **같은 자료 재분석 모달에서 `보관된 결과 열기`** — 주제 분석은 이걸로 0원입니다.
3. **그림이 많은 스캔 기출은 값이 크게 늘어납니다.** 강의록은 그림 속 글자 전사, 기출은 그림 설명에 각각 Vision 호출이 페이지 단위로 나갑니다.
4. **파일 수를 줄이세요.** 잘려서 버려지는 부분이 줄어 같은 값으로 더 깊게 읽습니다.
5. **한 번에 30문제**가 다섯 번에 6문제씩보다 쌉니다(분석 결과를 재사용하므로).
6. 전북대 게이트웨이를 쓴다면 **키 입력칸 아래에서 잔액을 미리 확인**하세요.

> ⚠️ `■ 중지`를 눌러도 **이미 나간 값은 돌아오지 않습니다.** 중지는 남은 호출을 막을 뿐입니다.

---

## 9. 🚨 자주 겪는 문제

**모델 목록이 안 떠요**
→ 대개 API 키가 틀렸습니다. 키를 다시 확인하고 `↻`를 누르세요. 제공사 선택이 키와 맞는지도 보세요.

**새로고침했더니 API 키가 비었어요**
→ 정상입니다. 키는 어디에도 저장하지 않습니다. 다시 입력하세요.

**생성하다 말고 멈췄어요 / 결과가 안 와요**
→ 탭을 닫거나 새로고침하면 중단됩니다. 자료가 아주 많으면(수백 쪽 + 스캔 이미지) **시간이 오래 걸려 연결이 끊길 수 있습니다.** 이 경우 파일을 줄여 다시 시도하세요.

**업로드가 거부돼요**
→ 강의자료 + 기출 **합계** 용량 제한과, **각각 7개** 제한을 확인하세요. PDF가 아닌 파일은 받지 않습니다.

**모바일에서 파일 선택 창이 비어 보여요**
→ PDF만 고를 수 있게 걸러진 상태입니다. 클라우드(드라이브 등)에 있는 파일은 먼저 기기에 내려받으면 잘 잡힙니다.

**기출 주제 분석 결과가 0개예요**
→ (1) 강의록과 기출이 같은 과목·같은 범위인지, (2) 스캔 이미지 PDF가 아닌지 확인하세요. 페이지·문항 번호를 못 읽으면 근거가 없어 제외됩니다.

**다른 기기에서 보니 기록이 없어요**
→ 게스트로 만든 기록은 그 브라우저에만 있습니다. 로그인 후 만든 것만 기기 간에 따라옵니다.

**정답이 맞는 것 같은데 오답 처리됐어요**
→ 골학은 대소문자·공백·하이픈·괄호를 무시하지만, 철자가 다른 동의어는 데이터에 등록된 것만 인정합니다. 억울한 사례는 팀에 알려 주세요(`static/data/bone_bank.json`에 정답을 추가하면 됩니다).

---

## 10. 🔒 내 데이터는 어떻게 다뤄지나

**API 키**
서버에도, 브라우저(localStorage·쿠키)에도 저장하지 않습니다. 요청에 실어 보내고 그걸로 끝입니다. 페이지를 켜 둔 동안 제공사별로 기억하는 것도 **메모리에만** 있어서, 새로고침하면 사라집니다.

**업로드한 PDF**
읽는 동안만 서버 디스크에 임시 사본을 두고(메모리를 아끼기 위한 것입니다), 텍스트 추출이 끝나면 **즉시 지웁니다.** 오류·중지로 삭제 단계를 못 밟은 찌꺼기도 **1시간 안에 자동으로 청소**됩니다. 원본 PDF는 어떤 형태로도 보관하지 않습니다.

**남는 것**
분석 *결과*입니다 — 핵심 개념·기출 형식 요약(세션), 생성된 문제, 주제 대응표. 여기에 더해, 같은 자료를 다시 올렸을 때 Vision 값을 두 번 내지 않도록 **그림 설명을 캐시해 둡니다.** 이것도 내 기록에 묶여 있고, 세션을 지우면 함께 정리됩니다.

**내 기록의 소유자**
로그인했다면 계정에, 로그인하지 않았다면 브라우저에 묶인 임시 신원에 저장됩니다. 어느 쪽이든 **다른 사용자에게는 보이지 않습니다.**

**골학 점수**
맞힘/정답률은 화면에서만 셉니다. 서버로 보내지 않고, 탭을 새로고침하면 초기화됩니다. 남기고 싶은 문제만 `🔖 오답에 넣기`로 직접 담으세요.

**삭제**
세션·시험지·분석 결과·오답 폴더는 각 화면의 🗑️로 직접 지울 수 있습니다. **세션을 지우면 그 세션에서 나온 생성 이력도 함께** 사라집니다.

**LLM 제공사로 나가는 것**
문제를 만들려면 자료 내용이 선택한 제공사(전북대 게이트웨이·OpenAI·Anthropic·Gemini)로 전송되어야 합니다. 이건 이 사이트가 아니라 **각 제공사의 정책**을 따릅니다. 민감한 자료라면 어느 제공사를 고를지 먼저 확인하세요.

---

## 관련 문서

| 문서 | 언제 보나 |
|---|---|
| [README.md](README.md) | 프로젝트 개요·로컬 실행 |
| [팀원_가이드.md](팀원_가이드.md) | 코드를 짜서 GitHub에 올릴 때 |
| [서버_가이드.md](서버_가이드.md) | 올린 코드를 실제 사이트에 반영할 때 |
| [CONTRIBUTING.md](CONTRIBUTING.md) | 브랜치·DB 스키마·기능 추가 규칙 |
