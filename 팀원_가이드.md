# 🚀 팀원 가이드 — 내 기능 만들어서 GitHub에 올리기

> 깃허브를 처음 써보는 사람도 **그대로 따라 하면 되는** 안내서입니다.
> 명령어는 회색 박스를 **복사해서 붙여넣기** 하면 됩니다. `이렇게_생긴_부분`만 자기 것으로 바꾸세요.
> (더 자세한 규칙은 [CONTRIBUTING.md](CONTRIBUTING.md) 참고)

---

## 🧠 0. 5분 개념 정리 (비유로 이해하기)

우리는 코드를 **혼자 쓰는 게 아니라 4명이 같이** 씁니다. 그래서 규칙이 필요해요.

| 용어 | 쉬운 뜻 (비유) |
|---|---|
| **저장소 (repository, repo)** | 우리 프로젝트 코드가 있는 **공용 폴더**. 원본은 GitHub에 있음. |
| **clone (클론)** | 그 공용 폴더를 **내 컴퓨터로 처음 복제**해 오는 것. (딱 1번) |
| **branch (브랜치)** | 원본을 안 건드리고 만드는 **"내 작업용 사본"**. `main`은 항상 정상 작동하는 원본. |
| **commit (커밋)** | 내 변경사항을 하나의 **저장 지점**으로 묶어 기록. (게임의 "세이브"⏺️) |
| **push (푸시)** | 내 컴퓨터의 커밋들을 **GitHub(공용)로 올리기** ⬆️ |
| **pull (풀)** | GitHub의 **최신 내용을 내 컴퓨터로 내려받기** ⬇️ |
| **Pull Request (PR)** | "내 브랜치를 `main`에 합쳐주세요"라고 **요청 + 팀원이 검토**하는 것. |

**핵심 그림 한 장:**
```
GitHub(공용 원본)  ──clone──▶  내 컴퓨터
       ▲                          │
       │ push ⬆️                   │ 내 브랜치에서 코드 작성 → commit(세이브)
       │                          ▼
       └──── Pull Request로 합치기 ◀── (팀원 리뷰 후 main에 merge)
```

> 💡 **가장 중요한 규칙 하나:** `main`에는 **직접 손대지 않습니다.** 항상 내 `feature/...` 브랜치에서 작업하고, PR로만 합칩니다.

---

## 🖱️ 더 쉬운 방법: GitHub Desktop (명령어가 부담되면)

명령어가 무섭다면 **GitHub Desktop**이라는 무료 앱을 쓰세요. 아래 모든 작업을 **버튼 클릭**으로 할 수 있습니다.
- 다운로드: <https://desktop.github.com>
- 개념은 똑같습니다. "pull = Fetch/Pull 버튼", "브랜치 = 상단 Current Branch", "commit = 왼쪽 아래 Commit 버튼", "push = Push origin 버튼", "PR = Create Pull Request 버튼".

이 문서는 **명령어 방식**으로 설명하지만, GitHub Desktop을 쓰면 같은 단계를 클릭으로 하면 됩니다. 편한 쪽을 고르세요.

---

## 🛠️ 1. 최초 1회 준비 (딱 한 번만)

### ① Git 설치
<https://git-scm.com/download/win> 에서 받아 설치 (계속 "다음"만 눌러도 됨).

### ② GitHub 계정 + 저장소 초대 받기
1. <https://github.com> 에서 계정 만들기.
2. **팀장에게 내 GitHub 아이디를 알려주고**, 저장소(`MCU-summer`)에 **Collaborator로 초대**받기. (초대 메일의 수락 버튼 클릭)

### ③ 저장소를 내 컴퓨터로 clone (복제)
원하는 폴더에서 터미널을 열고(폴더 주소창에 `cmd` 입력 후 Enter), 아래 실행:
```
git clone https://github.com/cuteCrongCrong/MCU-summer.git
cd MCU-summer
```
이제 `MCU-summer` 폴더가 생기고, 그 안이 우리 프로젝트입니다.

### ④ 내 정보 등록 (커밋에 이름 남기기, 1번만)
```
git config --global user.name "내이름"
git config --global user.email "내GitHub이메일"
```

### ⑤ 프로그램 실행 준비
- **`설치.bat` 더블클릭** → 필요한 파이썬 패키지 자동 설치.
- **`실행.bat` 더블클릭** → 앱 실행 (브라우저 자동으로 열림).
- (로그인 기능까지 테스트하려면 `secret_config.example.py`를 `secret_config.py`로 복사해 구글 자격증명을 채워야 함. 없어도 나머지는 잘 돌아감.)

---

## 🔁 2. 매일 하는 작업 흐름 (제일 중요! 이 순서만 지키면 됨)

### 1단계 — 작업 시작 전, 최신 내용 받기
```
git checkout main
git pull
```
> 왜? 팀원들이 올린 최신 코드를 먼저 받아야 충돌이 안 생깁니다. **작업 시작 전 항상 하세요.**

### 2단계 — 내 작업 브랜치로 이동
**처음 만들 때** (기능 이름을 영어로, 예: `feature/login`, `feature/note`):
```
git checkout -b feature/내기능이름
```
**이미 만든 브랜치로 돌아올 때:**
```
git checkout feature/내기능이름
```

### 3단계 — 코드 작성 ✍️
- **내 파일에서만** 작업하세요. (새 기능은 대부분 새 파일 추가 → 충돌 거의 없음. [CONTRIBUTING.md](CONTRIBUTING.md) 5번 참고)

### 4단계 — 자주 저장(커밋)하기
조금 만들 때마다 저장하는 습관을 들이세요:
```
git add -A
git commit -m "무엇을 했는지 한 줄 설명"
```
예: `git commit -m "feat: 로그인 화면 만들기"`

### 5단계 — 올리기 전에 main 최신 반영 (충돌 미리 방지)
```
git checkout main
git pull
git checkout feature/내기능이름
git merge main
```
> 충돌이 나면 → 아래 **"⚠️ 5. 충돌이 났을 때"** 를 보세요. 겁먹지 마세요!

### 6단계 — GitHub에 올리기(push)
```
git push -u origin feature/내기능이름
```
(두 번째부터는 그냥 `git push`)

### 7단계 — Pull Request(PR) 만들기
1. push하면 터미널에 뜨는 링크를 열거나, GitHub 저장소 페이지에 뜨는 **"Compare & pull request"** 버튼 클릭.
2. 제목·설명을 적고 **Create pull request**.
3. **팀원 1명이 리뷰**하고 문제 없으면 **Merge** 버튼으로 `main`에 합침.
4. 합쳐지면 그 브랜치는 끝. 다음 기능은 **1단계부터 새 브랜치**로 다시 시작.

---

## 🧩 3. 내 기능은 어디에 만드나? (충돌 안 나게)

새 기능은 **새 파일**로 만드는 게 원칙입니다. 그래야 남과 안 부딪혀요.

| 만드는 것 | 어디에 |
|---|---|
| 백엔드(서버) 기능 | `features/내기능.py` **새 파일** + `app.py`에 등록 **2줄** |
| 화면(프런트) 기능 | `static/js/내기능.js` **새 파일** + `index.html`에 `<script>` **1줄** |
| DB 테이블 필요 | `db.py`의 `init_db()`에 **내 테이블만 추가** |

> 예시와 자세한 방법은 [CONTRIBUTING.md](CONTRIBUTING.md) 7번(로그인 추가 예시)에 있습니다.

---

## 🚫 4. 절대 하면 안 되는 것 (사고 방지)

1. ❌ **`main`에 직접 커밋/푸시하기** → 항상 `feature/...` 브랜치 + PR.
2. ❌ **비밀값 올리기** — `secret_config.py`, API 키 파일 등. (`.gitignore`가 막아주지만, 커밋 전 `git status`로 그 파일이 **안 보이는지** 확인!)
3. ❌ **남의 파일 함부로 고치기** → 꼭 필요하면 그 담당자에게 먼저 말하기.
4. ❌ **작업을 몰아서 한 번에 올리기** → 조금씩 자주 커밋·푸시하고, **하루 한 번은 `git merge main`**.

---

## ⚠️ 5. 충돌(conflict)이 났을 때 — 당황 금지

충돌 = **같은 부분을 나와 팀원이 둘 다 고쳤다**는 뜻. 흔한 일이고 고칠 수 있습니다.

`git merge main` 했을 때 이런 게 뜨면:
```
CONFLICT (content): Merge conflict in index.html
```
1. 그 파일을 열면 이런 표시가 있습니다:
   ```
   <<<<<<< HEAD
   (내 코드)
   =======
   (팀원 코드)
   >>>>>>> main
   ```
2. **둘 중 맞는 걸 남기거나, 둘 다 살려 손으로 합치고**, `<<<<<<<` `=======` `>>>>>>>` **세 줄은 지웁니다.**
3. 저장 후:
   ```
   git add .
   git commit
   ```
- ❓ 판단이 안 서면 → **그 코드 짠 팀원에게 물어보세요.** 혼자 지우지 말기.
- 😱 너무 꼬였으면 → `git merge --abort` 로 **없던 일로 되돌리고** 다시 시도.

---

## 📋 6. 자주 쓰는 명령어 치트시트

| 하고 싶은 것 | 명령어 |
|---|---|
| 지금 상태 보기 (뭐가 바뀌었나) | `git status` |
| 최신 내용 받기 | `git pull` |
| 새 브랜치 만들고 이동 | `git checkout -b feature/이름` |
| 기존 브랜치로 이동 | `git checkout feature/이름` |
| 브랜치 목록 보기 | `git branch` |
| 변경 저장(커밋) | `git add -A` → `git commit -m "설명"` |
| GitHub에 올리기 | `git push` |
| 최근 기록 보기 | `git log --oneline` |
| 병합 취소 | `git merge --abort` |

---

## 🆘 7. 막혔을 때

- **에러 메시지를 그대로 복사**해서 팀 채팅에 공유하거나 검색하세요. (대부분 남들도 겪은 문제)
- 커밋 전에 항상 `git status` 로 **뭐가 올라갈지** 확인하는 습관.
- 정말 꼬였으면, **함부로 지우지 말고** 물어보세요. Git은 웬만하면 되돌릴 수 있습니다.

---

### ✅ 한 줄 요약
> **① `main`에서 pull → ② 내 브랜치로 → ③ 코드 → ④ commit → ⑤ merge main → ⑥ push → ⑦ PR로 합치기.** 이 7단계 반복!

---

## 🖥️ 그다음 — 내 코드를 실제 사이트에 올리기

이 앱은 인터넷에 배포돼 있습니다. 그런데 **PR이 `main`에 merge되는 것만으로는 사이트가 안 바뀝니다.**
서버에 코드를 가져오라고 한 번 더 시켜야 합니다.

그 방법과 서버 구조는 **[서버_가이드.md](서버_가이드.md)** 에 있습니다.
(서버 접속 준비 → 들어가기 → 코드 반영 → 문제 생겼을 때 보는 법)
