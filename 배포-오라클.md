# ☁️ Oracle Cloud 무료 배포 가이드

> ## 📌 지금은 이 문서가 필요 없습니다
>
> **이 앱은 Google Cloud에 배포돼 운영 중입니다.** 서버를 새로 만들 필요가 없어요.
>
> - 이미 올라간 사이트에 **내 코드를 반영**하려면 → **[서버_가이드.md](서버_가이드.md)**
> - GCP 서버를 처음부터 만드는 절차가 궁금하면 → [배포-GCP.md](배포-GCP.md)
>
> **그럼 이 문서는 왜 남아 있나** — GCP 무료 티어는 **RAM이 1GB뿐**이고 미국 리전에만
> 있습니다. 큰 PDF에서 메모리가 계속 모자라거나, 학교 LLM 게이트웨이가 미국 IP를
> 막게 되면 옮길 곳이 필요합니다. Oracle 무료 티어는 **서울 리전에 RAM 12GB** 라
> 그때의 탈출구입니다. 스크립트도 [deploy/oracle/](deploy/oracle/) 에 그대로 있습니다.
>
> 아래는 **그런 상황이 됐을 때** 읽으면 되는 내용입니다.

Oracle Cloud의 **Always Free** VM에 올리는 절차입니다. 완전 무료이고,
진짜 서버라서 **코드를 한 줄도 바꾸지 않고** SQLite를 그대로 씁니다.

전체 소요: 처음이면 40분~1시간 (대부분 가입·대기 시간)

> 일반적인 배포 개념·환경변수 설명은 [배포.md](배포.md)를 먼저 읽으세요.

---

## 준비물

- 신용/체크카드 (본인 확인용 — **청구되지 않습니다**)
- 휴대폰 번호
- GitHub에 코드가 push되어 있을 것

---

## 1단계 — Oracle Cloud 계정 만들기

1. <https://www.oracle.com/kr/cloud/free/> → **무료로 시작하기**
2. 국가는 **대한민국**, 이메일 인증
3. **홈 리전 선택이 가장 중요합니다.** 한 번 정하면 **못 바꿉니다.**
   - `South Korea Central (Seoul)` 또는 `Japan East (Tokyo)` 를 권장 (지연시간 짧음)
   - 다만 서울/도쿄는 ARM 무료 인스턴스 **용량이 꽉 차 있는 경우가 많습니다.**
     생성이 계속 실패하면 다른 리전으로 새 계정을 만들어야 하니, 아래 2단계를
     먼저 시도해보고 안 되면 판단하세요.
4. 카드 정보 입력 → 본인 확인 (실제 청구 없음, 소액 승인 후 취소)

> ⚠️ 계정 생성 직후에는 **"업그레이드" 안내가 계속 뜨는데 누르지 마세요.**
> Always Free 리소스만 쓰면 영원히 무료입니다.

---

## 2단계 — VM 인스턴스 만들기

콘솔 → **Compute** → **Instances** → **Create instance**

| 항목 | 설정 |
|---|---|
| Name | `mcu-server` (자유) |
| Image | **Ubuntu 24.04** (Canonical Ubuntu) |
| Shape | **VM.Standard.A1.Flex** (Ampere ARM) |
| OCPU / Memory | **2 OCPU / 12GB** ← Always Free 최대치 |
| Boot volume | 기본값 (50GB) |
| SSH keys | **Save private key** 눌러 `.key` 파일 저장 ⚠️ |

> **"Out of capacity" 오류가 나면** — 해당 리전에 ARM 용량이 없다는 뜻입니다.
> 시간대를 바꿔 (새벽 등) 재시도하거나, `VM.Standard.E2.1.Micro` (AMD, 1GB RAM)로
> 대신 만드세요. AMD 마이크로도 Always Free이고 이 앱은 충분히 돌아갑니다.
> 다만 PDF가 크면 메모리가 빠듯할 수 있습니다.

생성되면 **Public IP address**를 적어둡니다. (예: `152.67.xxx.xxx`)

---

## 3단계 — 클라우드 방화벽 열기 (콘솔)

Oracle은 방화벽이 **두 겹**입니다. 이 단계는 콘솔에서만 할 수 있습니다.

인스턴스 상세 → **Virtual cloud network** 링크 → **Security Lists**
→ `Default Security List` → **Add Ingress Rules**

아래 두 개를 추가합니다:

| Source CIDR | IP Protocol | Destination Port |
|---|---|---|
| `0.0.0.0/0` | TCP | `80` |
| `0.0.0.0/0` | TCP | `443` |

> 나머지 한 겹(인스턴스 내부 iptables)은 설치 스크립트가 알아서 엽니다.

---

## 4단계 — 도메인 연결 (무료)

HTTPS 인증서를 받으려면 IP가 아니라 **도메인**이 필요합니다.
Google 로그인도 도메인이 있어야 콜백을 등록할 수 있습니다.

<https://www.duckdns.org> 접속 → Google 계정으로 로그인 →
원하는 이름 입력 (예: `mcu-club`) → **add domain**
→ `current ip` 칸에 2단계의 **Public IP**를 넣고 **update ip**

이제 `mcu-club.duckdns.org` 가 서버를 가리킵니다. 무료이고 만료도 없습니다.

---

## 5단계 — 서버 접속

저장해둔 SSH 키로 접속합니다.

**Windows (PowerShell):**

```bash
icacls "$env:USERPROFILE\Downloads\ssh-key.key" /inheritance:r /grant:r "$($env:USERNAME):R"
```

```bash
ssh -i "$env:USERPROFILE\Downloads\ssh-key.key" ubuntu@<서버IP>
```

> 첫 줄은 키 파일 권한을 좁히는 명령입니다. 권한이 너무 열려 있으면
> SSH가 키를 거부합니다. (`UNPROTECTED PRIVATE KEY FILE` 오류)

---

## 6단계 — 설치 스크립트 실행

서버에 접속한 상태에서:

```bash
curl -fsSL https://raw.githubusercontent.com/cuteCrongCrong/MCU-summer/main/deploy/oracle/setup.sh -o setup.sh
```

```bash
sudo bash setup.sh mcu-club.duckdns.org
```

(마지막 인자를 **본인 도메인**으로 바꾸세요)

스크립트가 하는 일:

1. 파이썬·git·Caddy 설치
2. 전용 사용자 `mcu` 로 `/opt/mcu` 에 코드 설치
3. DB를 `/var/lib/mcu/sessions.db` 에 분리 (코드와 데이터 분리)
4. `FLASK_SECRET_KEY` 자동 생성 → `/etc/mcu/mcu.env`
5. systemd 등록 → **재부팅해도 자동 시작**
6. Caddy가 Let's Encrypt 인증서를 자동 발급 → **https 적용**
7. 인스턴스 iptables에 80·443 개방

끝나면 `https://mcu-club.duckdns.org` 로 접속됩니다.

> 인증서 발급에 10~60초 걸립니다. 바로 안 되면 잠깐 기다렸다 새로고침하세요.

---

## 7단계 — Google 로그인 연결 (선택)

로그인 없이도 모든 기능이 동작하므로 나중에 해도 됩니다.

**7-1.** Google Cloud Console → 사용자 인증 정보 → OAuth 클라이언트 ID
→ **승인된 리디렉션 URI** 에 추가:

```
https://mcu-club.duckdns.org/login/google/callback
```

기존 `http://localhost:5000/login/google/callback` 은 **지우지 마세요.**
로컬 개발에 계속 필요합니다.

**7-2.** 서버에서 값을 채웁니다:

```bash
sudo nano /etc/mcu/mcu.env
```

`GOOGLE_CLIENT_ID=` 와 `GOOGLE_CLIENT_SECRET=` 뒤에 값을 붙여넣고 저장
(`Ctrl+O` → `Enter` → `Ctrl+X`), 그다음:

```bash
sudo systemctl restart mcu
```

---

## 평소 운영

### 코드 업데이트 (로컬에서 push한 뒤)

```bash
sudo bash /opt/mcu/deploy/oracle/update.sh
```

DB와 설정은 건드리지 않고 코드만 갱신 후 재시작합니다.

### 상태·로그 확인

```bash
sudo systemctl status mcu
```

```bash
sudo journalctl -u mcu -f
```

### 백업 (권장)

```bash
sudo apt-get install -y sqlite3 && sudo bash /opt/mcu/deploy/oracle/backup.sh
```

매일 자동 백업하려면 `sudo crontab -e` 후 아래 한 줄 추가:

```
0 4 * * * /bin/bash /opt/mcu/deploy/oracle/backup.sh >> /var/log/mcu-backup.log 2>&1
```

> WAL 모드를 쓰므로 `.db` 파일만 복사하면 **깨진 백업**이 됩니다.
> 반드시 이 스크립트(내부적으로 `sqlite3 .backup` 사용)를 쓰세요.

---

## 문제 해결

| 증상 | 확인할 것 |
|---|---|
| 접속 자체가 안 됨 (타임아웃) | 3단계 Security List에 80·443을 넣었는지 |
| `502 Bad Gateway` | 앱이 죽음 → `sudo journalctl -u mcu -n 50` |
| 인증서 발급 실패 | DuckDNS IP가 서버 IP와 일치하는지, 80 포트가 열렸는지 |
| Google 로그인 `redirect_uri_mismatch` | 7-1의 URI를 **https로** 정확히 등록했는지 |
| 문제 생성 중 끊김 | `sudo journalctl -u mcu -n 100` 에서 실제 오류 확인 |
| 재부팅 후 안 뜸 | `sudo systemctl enable mcu` 확인 |

### 시크릿 키에 대한 경고

`/etc/mcu/mcu.env` 의 `FLASK_SECRET_KEY` 를 **바꾸지 마세요.**
바뀌는 순간 모든 쿠키 서명이 무효가 되어, 비로그인 사용자들이 자기가 만든
세션·오답노트에 영영 접근할 수 없게 됩니다.
(설치 스크립트를 다시 돌려도 기존 키는 그대로 유지됩니다)

---

## 비용이 정말 0인지

Always Free 리소스만 쓰면 무료입니다. 확인하려면
콘솔 → **Billing & Cost Management** → **Cost Analysis** 에서 $0 인지 보세요.

다만 Oracle은 2026년 6월 ARM 무료 한도를 4 OCPU/24GB → 2 OCPU/12GB로
**공지 없이** 축소한 전례가 있습니다. 한도가 또 바뀔 수 있다는 점은 감안하세요.
이 앱은 2 OCPU/12GB로 충분히 돌아갑니다.
