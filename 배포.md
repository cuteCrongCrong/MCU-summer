# 🚀 배포 가이드

로컬에서 쓰던 앱을 인터넷에 공개하는 절차입니다.
**로컬 개발 방식은 그대로입니다** — `실행.bat` 더블클릭은 계속 똑같이 동작합니다.

---

## 0. 배포 전에 확인할 것

### 전북대 LLM 게이트웨이가 학교 밖에서도 되는지 — ✅ 확인됨

앱의 기본 프로바이더는 `https://factchat-cloud.mindlogic.ai/v1/gateway` 입니다.
이게 **학교 IP에서만** 허용되도록 막혀 있으면 클라우드에 올린 순간 문제 생성이 실패합니다.

> **2026-07-28 확인: 미국 VPN에서도 정상 동작합니다.**
> 국내는 물론 해외 리전(GCP 무료 티어 등)에 올려도 문제없습니다.

학교 정책은 바뀔 수 있으니, 한참 뒤에 배포가 실패한다면 이 가정부터 다시 의심하세요.
확인 방법: 휴대폰 테더링(학교망 밖)으로 노트북을 연결한 뒤 로컬에서 문제 생성을 해봅니다.

그리고 막히더라도 배포가 무의미해지지는 않습니다 — 이 앱은 OpenAI·Anthropic·Gemini도
지원하고 API 키는 사용자가 화면에서 요청마다 넣으므로, 각자 자기 키로 우회할 수 있습니다(유료).

---

## 1. 로컬 vs 배포 — 무엇이 달라지나

| | 로컬 (`실행.bat`) | 배포 (`serve.py`) |
|---|---|---|
| 서버 | Flask 개발 서버 | waitress (WSGI) |
| 디버거 | 켜짐 | **꺼짐** (켜면 시작 거부) |
| 수신 주소 | 127.0.0.1 (내 PC만) | 0.0.0.0 (외부 공개) |
| 쿠키 | 일반 | `Secure` (https 전용) |
| DB 위치 | 프로젝트 폴더 | 영구 디스크 |
| 시크릿 키 | 기본값 허용 | **필수** (없으면 시작 거부) |

전환은 환경변수 `APP_ENV=production` 하나로 이뤄집니다.
[config.py](config.py)의 `check_production_ready()`가 위험한 조합이면 서버를 아예 띄우지 않습니다.

---

## 2. 환경변수

| 이름 | 필수 | 기본값 | 설명 |
|---|---|---|---|
| `APP_ENV` | 배포 시 ✅ | `development` | `production` 지정 시 배포 모드 |
| `FLASK_SECRET_KEY` | 배포 시 ✅ | 개발용 기본값 | 세션·게스트 쿠키 서명 키 |
| `DB_PATH` | 배포 시 ✅ | 프로젝트 폴더 | **영구 디스크** 경로여야 함 |
| `GOOGLE_CLIENT_ID` | | (없음) | 없으면 로그인 버튼만 숨겨짐 |
| `GOOGLE_CLIENT_SECRET` | | (없음) | " |
| `PORT` | | `5000` | Render 등은 자동 주입 |
| `HOST` | | 배포 시 `0.0.0.0` | |
| `TRUST_PROXY` | | 배포 시 켜짐 | 프록시 뒤 https 인식 |
| `SERVER_THREADS` | | `16` | 동시 처리 스레드 |
| `MAX_UPLOAD_MB` | | `100` | 업로드 상한 |

### 시크릿 키 만들기

```bash
python -c "import secrets;print(secrets.token_urlsafe(48))"
```

> ⚠️ **이 키는 한 번 정하면 바꾸지 마세요.** 키가 바뀌면 기존 쿠키 서명이 전부 무효가 되어
> 비로그인 사용자들이 자기 세션·오답노트에 접근할 수 없게 됩니다.

---

## 3. 어디에 올릴까

세 가지 선택지가 준비돼 있습니다. 셋 다 **코드 변경 없이** SQLite를 그대로 씁니다.

| | 비용 | RAM | 리전 | 가이드 |
|---|---|---|---|---|
| **Oracle Cloud** (권장) | 무료 | 12GB | 서울 선택 가능 | [배포-오라클.md](배포-오라클.md) |
| **Google Cloud** | 무료 | **1GB** | 미국만 | [배포-GCP.md](배포-GCP.md) |
| **Render** | 월 $7~ | 512MB~ | 싱가포르 | 아래 3-1 |

**Oracle이 기본 추천입니다.** 무료인데 서울 리전에 RAM도 넉넉합니다.
다만 ARM 인스턴스 용량이 없어 생성이 계속 실패하는 경우가 있습니다.

**GCP는** 구글 로그인 자격증명을 이미 Google Cloud Console에서 쓰고 있어
**관리 지점이 하나로 합쳐진다**는 게 장점입니다. 무료 티어가 미국 리전뿐이라 걱정했던
게이트웨이 차단은 위 0절대로 **확인 결과 문제없습니다.** 남는 제약은 RAM 1GB —
큰 PDF에서 메모리가 빠듯합니다. (가이드에 대응 방법이 있습니다)

**Render는** 서버 관리를 아예 안 하고 싶을 때의 선택지입니다.
**무료** 플랜은 영구 디스크를 붙일 수 없어 이 앱에는 쓸 수 없습니다 — 반드시 유료 플랜이어야 합니다.

## 3-1. Render 배포 절차 (유료)

### 3-1. 저장소 연결

1. GitHub에 코드를 push합니다.
2. Render → **New** → **Blueprint** → 저장소 선택
   → [render.yaml](render.yaml)이 자동으로 읽힙니다.

> Blueprint 대신 **New → Web Service**로 직접 만들어도 됩니다. 그때는
> Build Command `pip install -r requirements.txt`,
> Start Command `python serve.py` 를 넣고, 아래 값들을 손으로 설정하세요.

### 3-2. 유료 플랜 + 디스크 확인

`sessions.db`가 로컬 파일이라 **영구 디스크가 반드시 필요합니다.**
free 플랜은 디스크를 못 붙여서 재배포·재시작마다 모든 사용자 데이터가 사라집니다.

- 플랜: **Starter** 이상
- 디스크: `/var/data` 에 마운트 (render.yaml에 이미 지정됨)
- `DB_PATH=/var/data/sessions.db`

### 3-3. 구글 OAuth 리다이렉트 URI 등록

배포 주소가 나오면 (`https://<앱이름>.onrender.com`) Google Cloud Console에서
**승인된 리디렉션 URI**에 추가합니다:

```
https://<앱이름>.onrender.com/login/google/callback
```

기존 로컬 주소(`http://localhost:5000/login/google/callback`)는 **지우지 말고 같이 두세요.**
로컬 개발에서 계속 필요합니다.

> `TRUST_PROXY`가 켜져 있어야 콜백 주소가 `https://`로 만들어집니다.
> 배포 모드에서는 자동으로 켜집니다.

---

## 4. 다른 곳에 배포한다면

`serve.py`는 특정 플랫폼에 묶여 있지 않습니다. 다음만 지키면 어디든 됩니다.

- **요청 타임아웃이 없거나 5분 이상**일 것
  문제 생성은 LLM을 여러 번 연달아 호출해 **1~3분**이 걸립니다.
  Heroku(30초), Vercel/Netlify 서버리스(10~60초)는 이 때문에 못 씁니다.
- **파일시스템이 유지될 것** (SQLite를 쓰므로)
- **외부 HTTPS 호출이 자유로울 것** (PythonAnywhere 무료는 화이트리스트 때문에 불가)

### 직접 서버(VM)에 올리는 경우

```bash
pip install -r requirements.txt

export APP_ENV=production
export FLASK_SECRET_KEY="<생성한 키>"
export DB_PATH=/srv/mcu/sessions.db
export PORT=5000

python serve.py
```

앞단에 nginx를 두고 HTTPS를 종료시킨 뒤 5000번으로 넘기면 됩니다.
이때 `TRUST_PROXY=1`이 켜져 있어야 하며, nginx가
`X-Forwarded-Proto`, `X-Forwarded-Host`를 넘겨주도록 설정해야 합니다.

---

## 5. 배포 후에도 개발은 계속

배포는 끝이 아니라 파이프라인입니다.

1. 로컬에서 개발 (`실행.bat`)
2. 테스트
3. `git push`
4. Render가 자동으로 재배포

### DB 스키마를 바꿀 때

[db.py](db.py)의 `init_db()`가 앱 시작마다 실행되고, `_ensure_column()`이
없는 컬럼을 자동으로 추가합니다. **컬럼 추가는 거기에 한 줄 넣으면 끝**입니다.

반면 **컬럼 삭제·이름 변경·테이블 재구조화**는 자동화되지 않습니다.
실제 사용자 데이터가 쌓인 뒤에는 마이그레이션을 직접 짜야 하므로,
구조는 되도록 초기에 확정하세요.

### 데이터 백업

SQLite는 파일 하나라 백업이 간단합니다. 디스크에 접속해서:

```bash
sqlite3 /var/data/sessions.db ".backup '/var/data/backup.db'"
```

> WAL 모드를 쓰므로 `.db` 파일만 복사하면 안 됩니다.
> 반드시 위의 `.backup` 명령을 쓰세요 (`-wal` 파일에 아직 반영 안 된 내용이 있음).

---

## 6. 문제가 생기면

| 증상 | 원인 |
|---|---|
| 시작하자마자 `FLASK_SECRET_KEY가 기본값입니다` | 키 환경변수 누락 — 정상 동작(막는 게 맞음) |
| 구글 로그인 시 `redirect_uri_mismatch` | 배포 주소를 Console에 등록 안 함, 또는 `TRUST_PROXY` 꺼짐 |
| 재배포하면 데이터가 사라짐 | `DB_PATH`가 영구 디스크가 아님 |
| 문제 생성 중 연결 끊김 | 플랫폼의 요청 타임아웃 — 타임아웃 없는 곳으로 옮겨야 함 |
| `database is locked` | 스레드 과다 — `SERVER_THREADS`를 낮추세요 |
| 로그인 버튼이 안 보임 | `GOOGLE_CLIENT_ID/SECRET` 미설정 (의도된 동작) |
