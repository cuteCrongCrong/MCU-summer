# 🌐 Google Cloud 무료 배포 가이드

Google Cloud에 **공짜로** 서버를 하나 얻어서 이 앱을 올리는 방법입니다.
클라우드를 한 번도 안 써봤어도 따라올 수 있게 썼습니다.

**전체 소요: 처음이면 40분~1시간** (대부분 가입·대기 시간)

---

## 0. 이게 대체 뭘 하는 건가

지금은 `실행.bat` 을 더블클릭하면 **내 노트북 안에서만** 앱이 돌아갑니다.
그래서 내 노트북을 끄면 끝이고, 팀원은 접속할 수 없습니다.

배포는 이걸 **인터넷 어딘가에 24시간 켜져 있는 컴퓨터로 옮기는 일**입니다.
그러면 주소만 알면 누구나, 아무 때나, 자기 폰으로도 들어옵니다.

### 다 끝나면 이런 모양이 됩니다

```
        팀원 브라우저  (누구나 접속)
              │
              │  https://mcu-club.duckdns.org      ← 4단계에서 만드는 주소
              ▼
        방화벽 80 · 443  (대문)                     ← 3단계에서 여는 문
              │
   ┌──────────▼───────────────────────┐
   │  GCP VM  ·  미국  ·  24시간 켜짐   │           ← 2단계에서 빌리는 컴퓨터
   │                                   │
   │   Caddy      https 자물쇠 자동      │  ┐
   │     ↓                             │  │
   │   앱 (serve.py)  ──────────────────┼──┼──→  LLM 게이트웨이 (외부)
   │     ↓                             │  │
   │   sessions.db   데이터가 쌓이는 곳   │  ┘ 6단계 스크립트가 이 셋을 자동 설치
   └───────────────────────────────────┘
```

### 7단계가 각각 뭘 하는가

| 단계 | 한마디로 | 걸리는 시간 |
|---|---|---|
| 1 | Google Cloud 계정 준비 + **돈 새면 알림 오게** | 10분 |
| 2 | **컴퓨터 한 대 빌리기** | 2분 |
| 3 | 그 컴퓨터의 **대문 열기** | 1분 |
| 4 | 숫자 주소 대신 **이름 붙이기** | 5분 |
| 5 | 빌린 컴퓨터에 **원격으로 들어가기** | 2분 |
| 6 | **설치 스크립트 한 줄** — 실제 설치는 전부 여기서 | 5분 |
| 7 | Google 로그인 연결 (선택) | 5분 |

---

## 1. 용어 — 이 문서에 나오는 말들

낯선 단어가 나오면 여기로 돌아오세요.

| 말 | 쉽게 말하면 |
|---|---|
| **VM**, **인스턴스** | Google한테 컴퓨터 한 대 빌리는 것. 진짜 컴퓨터처럼 켜지고 계속 돌아갑니다 |
| **`e2-micro`** | 그 컴퓨터의 사양 이름. 제일 작은 것 — 그래서 무료 |
| **리전**, **`us-west1`** | 그 컴퓨터가 실제로 놓인 장소. 미국 오리건주 |
| **존**, **`us-west1-b`** | 리전 안의 세부 위치. 아무거나 골라도 되고, 이 문서는 `-b` 를 씁니다 |
| **부팅 디스크** | 그 컴퓨터의 하드디스크. **느린 것(`pd-standard`) = 무료**, 빠른 것 = 유료 |
| **방화벽** | 컴퓨터의 대문. GCP는 **기본이 전부 잠겨** 있어서 열어줘야 합니다 |
| **포트 `80` · `443`** | 웹사이트가 쓰는 문 번호. 80 = http, 443 = https(자물쇠) |
| **SSH** | 빌린 컴퓨터에 원격으로 들어가 명령어를 치는 것 |
| **도메인** | `mcu-club.duckdns.org` 같은 주소 이름 |
| **Caddy** | 대문 안쪽 문지기. https 자물쇠를 **자동으로 받아다 붙여줍니다** |
| **`sudo`** | "관리자 권한으로 실행" 이라는 뜻. 리눅스 명령어 앞에 붙입니다 |
| **프로젝트 ID** | Google Cloud에서 내 프로젝트를 가리키는 고유 문자열 |

---

## 2. 시작 전 — 이건 이미 확인됐습니다

이 앱은 기본적으로 전북대 LLM 게이트웨이(`factchat-cloud.mindlogic.ai`)로 문제를 만듭니다.
**GCP 무료 서버는 미국에만 있어서**, 그 게이트웨이가 한국에서만 열린다면
서버를 다 만들어놓고도 문제 생성이 안 될 뻔했습니다.

> **2026-07-28 확인: 미국 VPN에서 문제 생성이 정상 동작합니다.**
> 걱정 없이 이 가이드를 따라가시면 됩니다.

한참 뒤에 이 문서를 보고 계시고 배포한 서버에서 문제 생성이 계속 실패한다면,
학교 정책이 바뀌었을 수 있습니다. 그때는 [게이트웨이가 막혔을 때](#게이트웨이가-막혔을-때) 를 보세요.
(설치 스크립트도 마지막에 이 연결을 자동으로 점검하고 알려줍니다)

---

## 3. 준비물

**① 신용/체크카드**
무료로만 써도 카드 등록은 필요합니다. 무료 조건만 지키면 **실제로 청구되지 않습니다.**

**② GitHub에 코드가 올라가 있을 것**
서버가 GitHub에서 코드를 내려받는 방식이라, 로컬에만 있으면 안 됩니다.

**③ 내 PC에 gcloud CLI**
Google Cloud를 명령어로 조작하는 프로그램입니다.

<https://cloud.google.com/sdk/docs/install> 에서 Windows 설치 파일을 받아 실행하세요.
설치가 끝나면 **새 PowerShell 창을 열고** 아래로 로그인합니다.

```bash
gcloud auth login
```

브라우저가 열리면 Google 계정으로 로그인하고 권한을 허용하면 됩니다.

> 웹 콘솔에서 마우스로만 해도 되지만, **명령어를 복붙하는 쪽이 훨씬 안전합니다.**
> 무료 조건을 손으로 맞추다 틀리면 돈이 나가거든요.

---

## 4. 돈이 새지 않게 — 무료 조건 3가지

Google Cloud의 "Always Free"는 **정확히 정해진 사양에서만** 공짜입니다.
아래 셋 중 하나라도 어긋나면 그 순간부터 과금됩니다.

| 항목 | 반드시 이 값 | 어기면 |
|---|---|---|
| **리전** | `us-west1` / `us-central1` / `us-east1` | 서울 등 다른 곳은 **전액 과금** |
| **머신 타입** | `e2-micro` | 한 단계만 올려도 과금 |
| **부팅 디스크** | `pd-standard`, 30GB 이하 | **기본값이 유료 디스크입니다** ← 가장 흔한 실수 |

**세 번째가 진짜 함정입니다.** 웹 콘솔에서 VM을 만들면 디스크 종류가 기본으로
"균형 있는 영구 디스크"(유료)로 잡혀 있습니다. 손으로 "표준 영구 디스크"로 바꿔야 합니다.
→ 2단계의 명령어를 그대로 복붙하면 이 실수는 아예 안 납니다.

**데이터 전송량(egress)은 월 1GB까지 무료입니다.** 화면을 보는 것만으로는 절대 안 닿습니다 —
한 번 불러오는 데 약 30KB라 월 2만 번을 넘겨야 합니다.

**닿을 수 있는 건 그림입니다.** 서버가 PDF에서 뽑은 그림을 LLM 게이트웨이로 올릴 때
그게 전부 egress로 잡힙니다. 그림은 base64로 부풀려 보내므로 실제 전송량은 파일보다 33% 큽니다.

| 한 회차에 그림을 상한까지 읽으면 | 160장 (e2-micro 값) | 500장 (코드 기본값) |
|---|---|---|
| 벡터 슬라이드·텍스트 PDF 위주 (장당 0.3~0.6MB) | 약 100MB | 약 300MB |
| **스캔본 위주** (장당 2~3MB) | **약 500MB** | **1.6GB — 한 회차로 초과** |

스캔본을 통째로 읽히면 회차 한두 번에 한도에 닿습니다. 넘어도 금액은 몇 백 원
수준이지만, "무료"를 지키려면 알고는 있어야 합니다.

세 가지가 완충 역할을 합니다 — ① 같은 PDF를 다시 분석하면 **그림 호출이 0회**입니다
(결과를 캐시해 둡니다) ② 그림이 없는 PDF는 애초에 안 보냅니다 ③ 상한을 꽉 채우는 회차
자체가 드뭅니다.

실제로 얼마나 나갔는지는 결제 화면의 **네트워크 송신** 항목에서 봅니다. 부담스러우면
`IMAGE_CAP_EXAM` 부터 낮추세요 — 스캔 기출이 장당 용량이 제일 큽니다.

한국에서 가장 가까운 무료 리전은 **`us-west1`(오리건)** 입니다. 이 문서는 이걸 씁니다.

---

# 실제 절차

## 1단계 — 계정 준비 + 돈 알림 걸기

### 왜 하나

카드를 등록해야 VM을 만들 수 있고, 예산 알림은 **실수했을 때 한 달 뒤 청구서가 아니라
며칠 안에 메일로 알게** 해주는 안전장치입니다.

### 어떻게 하나

**1-1. 프로젝트 고르기**

<https://console.cloud.google.com> 에 접속합니다.
화면 왼쪽 위, Google Cloud 로고 옆의 **프로젝트 선택 드롭다운**을 누릅니다.

> 💡 **Google 로그인(OAuth) 자격증명을 만들어둔 그 프로젝트를 그대로 쓰는 걸 권합니다.**
> 7단계에서 콘솔을 왔다 갔다 안 해도 됩니다.
> 새로 만들려면 **새 프로젝트** → 이름 입력 → 만들기.

프로젝트를 고른 뒤, 드롭다운에 보이는 **프로젝트 ID** 를 메모해두세요.
(이름과 다를 수 있습니다. 보통 `mcu-summer-412345` 처럼 생겼습니다)

**1-2. 결제 계정 연결**

왼쪽 메뉴(≡) → **결제** → 안내에 따라 카드를 등록합니다.
처음이면 $300 무료 크레딧 안내가 뜨는데, 받아도 되고 안 받아도 됩니다.

**1-3. 예산 알림 걸기 — 건너뛰지 마세요**

**결제** → 왼쪽 **예산 및 알림** → **예산 만들기**

- 이름: 아무거나 (`mcu-경보`)
- 금액: **$1**
- 알림 임계값: 50% / 90% / 100% (기본값 그대로 두면 됩니다)

→ **완료**

**1-4. Compute Engine 켜기**

왼쪽 메뉴 → **Compute Engine** → **VM 인스턴스** 를 누르면
"Compute Engine API를 사용 설정하시겠습니까?" 가 뜹니다. **사용 설정** 을 누르세요.
1~2분 걸립니다.

### 됐는지 확인

**Compute Engine → VM 인스턴스** 화면이 (비어 있는 채로) 열리면 준비 완료입니다.

---

## 2단계 — 컴퓨터 빌리기

### 왜 하나

이 앱이 24시간 돌아갈 실제 서버를 만드는 단계입니다.

### 어떻게 하나

**내 PC의 PowerShell** 에서 아래를 실행합니다.
`<프로젝트ID>` 를 1-1에서 메모한 값으로 바꾸세요.

```bash
gcloud compute instances create mcu-app --project=<프로젝트ID> --zone=us-west1-b --machine-type=e2-micro --image-family=ubuntu-2404-lts-amd64 --image-project=ubuntu-os-cloud --boot-disk-type=pd-standard --boot-disk-size=30GB --tags=http-server,https-server
```

**이 긴 명령어가 하는 말:**

| 부분 | 뜻 |
|---|---|
| `instances create mcu-app` | `mcu-app` 이라는 이름으로 컴퓨터를 만들어라 |
| `--zone=us-west1-b` | 미국 오리건에 (무료 조건) |
| `--machine-type=e2-micro` | 제일 작은 사양으로 (무료 조건) |
| `--image-family=ubuntu-2404-lts-amd64` | 운영체제는 우분투 24.04로 |
| `--boot-disk-type=pd-standard` | **느린 디스크로 (무료 조건)** |
| `--boot-disk-size=30GB` | 용량 30GB (무료 한도) |
| `--tags=http-server,https-server` | 이 컴퓨터에 "웹서버" 라벨을 붙여라 (3단계에서 씀) |

### 됐는지 확인

명령이 끝나면 표가 하나 출력됩니다. 거기서 **`EXTERNAL_IP`** 열의 값을 메모하세요.

```
NAME     ZONE        MACHINE_TYPE  ...  EXTERNAL_IP    STATUS
mcu-app  us-west1-b  e2-micro      ...  34.82.___.___  RUNNING
```

이 숫자가 **인터넷에서 이 컴퓨터를 가리키는 주소**입니다. 4단계에서 씁니다.

### 알아둘 것

**이 IP는 컴퓨터를 껐다 켜면 바뀝니다.** (재부팅만으로는 안 바뀝니다)
고정 IP를 예약할 수도 있지만 그건 돈이 들 수 있어서, 대신 6단계에서
주소를 자동으로 따라가게 만드는 방법을 씁니다.

### 막히면

- **"API가 사용 설정되지 않았습니다"** → 1-4를 안 한 것입니다.
- **"프로젝트를 찾을 수 없습니다"** → 프로젝트 ID가 틀렸습니다. 이름 말고 **ID** 입니다.
- **할당량(quota) 오류** → 결제 계정이 연결 안 됐을 때 자주 납니다. 1-2를 확인하세요.

---

## 3단계 — 대문 열기

### 왜 하나

2단계에서 만든 컴퓨터는 **아직 아무도 접속할 수 없습니다.**
GCP는 기본적으로 모든 문을 잠가두거든요. 웹에 필요한 문 두 개를 엽니다.

### 어떻게 하나

내 PC의 PowerShell에서:

```bash
gcloud compute firewall-rules create mcu-allow-web --allow=tcp:80,tcp:443 --target-tags=http-server,https-server
```

2단계에서 컴퓨터에 `http-server`, `https-server` 라벨을 붙여뒀기 때문에
이 규칙이 자동으로 그 컴퓨터에 적용됩니다.

### 됐는지 확인

```bash
gcloud compute firewall-rules list --filter="name=mcu-allow-web"
```

`ALLOW` 열에 `tcp:80,tcp:443` 이 보이면 됩니다.

### 막히면

- **"이미 존재합니다"** → 전에 만든 규칙이 있는 것이니 그냥 넘어가면 됩니다.

---

## 4단계 — 주소 붙이기

### 왜 하나

두 가지 때문에 **숫자 IP가 아니라 이름이 필요**합니다.

1. **https 자물쇠는 숫자 주소로는 못 받습니다.** 인증서를 발급하는 기관이 이름을 요구합니다.
2. **Google 로그인**도 "이 주소로 돌아와도 된다"고 등록해야 하는데, 숫자 IP는 바뀔 수 있어 곤란합니다.

DuckDNS가 그 이름을 무료로, 만료 없이 줍니다.

### 어떻게 하나

**4-1.** <https://www.duckdns.org> 접속 → 위쪽의 **google** 버튼으로 로그인

**4-2.** 화면 가운데 입력칸에 원하는 이름을 넣습니다. (예: `mcu-club`)
→ **add domain** 클릭

이제 `mcu-club.duckdns.org` 가 내 것이 됩니다.

**4-3.** 방금 추가된 줄의 **`current ip`** 칸에 2단계에서 메모한 **EXTERNAL_IP** 를 넣고
→ **update ip** 클릭

**4-4.** 화면 위쪽의 **`token`** 값을 복사해서 메모해두세요.
(6단계에서 IP 자동 갱신에 씁니다. 선택 사항이지만 해두는 게 좋습니다)

### 됐는지 확인

내 PC의 PowerShell에서:

```bash
nslookup mcu-club.duckdns.org
```

`Address:` 에 2단계의 EXTERNAL_IP가 나오면 성공입니다.

### 막히면

- **주소가 안 나오거나 다른 IP가 나옴** → DNS가 퍼지는 데 1~2분 걸립니다. 잠시 후 다시.
- **`add domain` 이 안 먹힘** → 이미 남이 쓰는 이름입니다. 다른 이름으로 바꾸세요.

---

## 5단계 — 빌린 컴퓨터에 들어가기

### 왜 하나

이제부터의 작업(6단계)은 **그 컴퓨터 안에서** 해야 합니다.

### 어떻게 하나

내 PC의 PowerShell에서:

```bash
gcloud compute ssh mcu-app --zone=us-west1-b
```

처음 실행하면:
- SSH 키를 만들지 물어봅니다 → **Y**
- 키 암호(passphrase)를 물어봅니다 → **그냥 Enter 두 번** (비워둬도 됩니다)

### 됐는지 확인

명령 프롬프트가 이렇게 바뀌면 성공입니다. **여기서부터는 미국에 있는 컴퓨터입니다.**

```
사용자이름@mcu-app:~$
```

### 막히면

- **명령이 아예 안 되거나 오래 멈춤** → 웹 콘솔에서 접속하세요.
  **Compute Engine → VM 인스턴스** → `mcu-app` 줄의 **SSH** 버튼 → 브라우저 창이 열립니다.
  이 방법이 더 쉬울 수도 있습니다.
- **권한 오류** → `gcloud auth login` 을 다시 해보세요.

---

## 6단계 — 설치 스크립트 한 줄

### 왜 하나

**여기가 실제로 다 하는 곳입니다.** 파이썬 설치, 코드 내려받기, https 설정,
데이터베이스 자리 만들기, 재부팅해도 자동으로 켜지게 등록하기 — 전부 자동입니다.

> ### ⚠️ 먼저 — 배포 파일이 `main` 에 있어야 합니다
>
> 아래 명령은 `main` 브랜치에서 스크립트를 받아옵니다. 그런데 배포 관련 파일
> (`serve.py`·`deploy/`)이 아직 `feature/deploy` 브랜치에만 있다면 **첫 줄부터 404** 가 납니다.
>
> **정석** — `feature/deploy` 를 PR로 `main` 에 합친 뒤 아래를 그대로 실행합니다.
>
> **합치기 전에 먼저 시험해보려면** — 아래 두 명령 대신 이 두 줄을 쓰세요.
>
> ```
> curl -fsSL https://raw.githubusercontent.com/cuteCrongCrong/MCU-summer/feature/deploy/deploy/gcp/setup.sh -o setup.sh
> sudo env BRANCH=feature/deploy bash setup.sh mcu-club.duckdns.org
> ```
>
> 나중에 합친 뒤에는 `sudo env BRANCH=main bash /opt/mcu/deploy/gcp/update.sh` 로 되돌립니다.
>
> (`sudo env VAR=값` 형태인 이유 — `sudo VAR=값` 은 서버 설정에 따라 값이 무시될 수 있습니다)

### 어떻게 하나

**5단계에서 접속한 상태**(프롬프트가 `@mcu-app` 인 상태)에서 실행합니다.

**6-1. 스크립트 내려받기**

```bash
curl -fsSL https://raw.githubusercontent.com/cuteCrongCrong/MCU-summer/main/deploy/gcp/setup.sh -o setup.sh
```

아무 메시지도 안 나오면 성공입니다.

**6-2. 실행** — `mcu-club.duckdns.org` 를 **본인 도메인**으로 바꾸세요.

4단계에서 토큰을 메모했다면 이쪽을 쓰세요. IP가 바뀌어도 주소가 자동으로 따라갑니다.

```bash
sudo env DUCKDNS_TOKEN=여기에토큰 bash setup.sh mcu-club.duckdns.org
```

토큰이 없거나 나중에 하고 싶으면 이것만으로도 됩니다.

```bash
sudo bash setup.sh mcu-club.duckdns.org
```

### 진행되는 동안 화면에 나오는 것

8단계가 순서대로 지나갑니다. 3~5분 걸립니다.

| 화면에 나오는 단계 | 하는 일 |
|---|---|
| `1/8 시스템 패키지 설치` | 파이썬·git 등 기본 도구 |
| `2/8 swap 2GB 생성` | 메모리 부족으로 죽지 않게 하는 안전망 |
| `3/8 Caddy 설치` | https 자물쇠를 자동으로 받아올 문지기 |
| `4/8 앱 사용자·디렉터리` | 앱 전용 계정을 따로 만들어 권한을 좁힘 |
| `5/8 코드 내려받기` | GitHub에서 코드를 가져와 설치 |
| `6/8 환경변수 파일` | 비밀 키 자동 생성. **여기서 만든 키는 절대 바꾸지 마세요** |
| `7/8 systemd + Caddy` | 재부팅해도 자동으로 켜지게 등록 |
| `8/8 게이트웨이 점검` | LLM 서버에 닿는지 확인 |

### 됐는지 확인

마지막에 이런 상자가 나옵니다.

```
────────────────────────────────────────────────────────
  접속 주소:  https://mcu-club.duckdns.org
  DB 파일  :  /var/lib/mcu/sessions.db
  설정 파일:  /etc/mcu/mcu.env
  업로드 상한/스레드: 200MB / 4 (e2-micro 기준)
  이미지 상한: 강의록 50장 / 기출 30장 (한쪽 전체)
────────────────────────────────────────────────────────
```

그 아래에 "다음 할 일" 안내가 몇 줄 더 나오는데, 7단계에서 할 내용이니 지금은 넘어가도 됩니다.

**브라우저에서 그 주소로 들어가 보세요.** 앱 화면이 뜨고 주소창에 자물쇠가 보이면 성공입니다.

> 자물쇠(인증서) 발급에 10~60초 걸립니다. 바로 안 되면 잠깐 기다렸다 새로고침하세요.

### 막히면

- **접속이 아예 안 됨 (계속 로딩)** → 3단계 방화벽을 안 만들었을 가능성이 큽니다.
- **`502 Bad Gateway`** → 앱이 죽은 것입니다. 아래로 원인을 봅니다.

  ```bash
  sudo journalctl -u mcu -n 50 --no-pager
  ```

- **자물쇠가 안 붙고 경고가 뜸** → 4단계의 IP가 실제 VM IP와 다를 수 있습니다.
  `nslookup` 으로 다시 확인하세요.
- **`8/8` 에서 게이트웨이 경고가 나옴** → [게이트웨이가 막혔을 때](#게이트웨이가-막혔을-때) 참고.

---

## 7단계 — Google 로그인 연결 (선택)

### 왜 하나

로그인 없이도 **모든 기능이 동작합니다.** 게스트로도 자기 세션과 오답노트가 따로 보관돼요.
로그인을 붙이면 기기를 바꿔도 같은 데이터를 볼 수 있습니다. 나중에 해도 됩니다.

### 어떻게 하나

**7-1. Google에 새 주소를 등록합니다**

<https://console.cloud.google.com> → 왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보**
→ OAuth 2.0 클라이언트 ID 목록에서 이 앱의 것을 클릭
→ **승인된 리디렉션 URI** 의 **URI 추가** 를 누르고 아래를 넣습니다.

```
https://mcu-club.duckdns.org/login/google/callback
```

→ **저장**

> ⚠️ 기존의 `http://localhost:5000/login/google/callback` 은 **지우지 마세요.**
> 로컬에서 개발할 때 계속 필요합니다. 둘 다 있어도 아무 문제 없습니다.

같은 화면에서 **클라이언트 ID** 와 **클라이언트 보안 비밀번호** 를 복사해둡니다.

**7-2. 서버에 값을 넣습니다**

서버에 접속한 상태(5단계)에서:

```bash
sudo nano /etc/mcu/mcu.env
```

`nano` 라는 편집기가 열립니다. 방향키로 내려가서 아래 두 줄 뒤에 값을 붙여넣습니다.

```
GOOGLE_CLIENT_ID=여기에_클라이언트_ID
GOOGLE_CLIENT_SECRET=여기에_보안_비밀번호
```

저장하고 나가기: **`Ctrl+O`** → **`Enter`** → **`Ctrl+X`**

**7-3. 앱 다시 시작**

```bash
sudo systemctl restart mcu
```

### 됐는지 확인

사이트를 새로고침하면 **로그인 버튼이 나타납니다.** 눌러서 Google 계정으로 로그인해보세요.

### 막히면

- **`redirect_uri_mismatch` 오류** → 7-1의 주소가 정확하지 않습니다.
  `https` 인지, 끝에 `/login/google/callback` 이 붙었는지, 오타가 없는지 보세요.
- **로그인 버튼이 안 보임** → 7-2의 값이 비어 있거나 7-3을 안 한 것입니다.

---

# 배포 후

## 잘 됐는지 최종 점검

| # | 확인할 것 | 방법 | 기대 |
|---|---|---|---|
| 1 | 사이트 접속 | 브라우저로 `https://<도메인>` | 앱 화면 + 자물쇠 |
| 2 | 서버 상태 | `sudo systemctl status mcu` | `active (running)` |
| 3 | 메모리 안전망 | `free -h` | Swap 2.0Gi |
| 4 | **문제 생성** | PDF 올려서 끝까지 생성 | 문제가 나옴 |
| 5 | 큰 파일 | 100MB 넘는 PDF로 생성 | 끊기지 않고 완주 (`free -h`로 메모리도 확인) |
| 6 | 게스트 데이터 | 비로그인으로 오답노트 저장 → 브라우저 껐다 재접속 | 데이터가 남아 있음 |
| 7 | 재부팅 복구 | `sudo reboot` 후 2분 뒤 재접속 | 저절로 다시 떠 있음 |
| 8 | 비용 | 며칠 뒤 콘솔 → 결제 → 보고서 | $0 |

## 평소 운영

### 코드를 고쳤을 때 서버에 반영하기

로컬에서 GitHub에 push한 다음, 서버에 접속해서:

```bash
sudo bash /opt/mcu/deploy/gcp/update.sh
```

**데이터베이스와 설정은 건드리지 않고** 코드만 갱신하고 재시작합니다.

### 뭔가 이상할 때 로그 보기

```bash
sudo journalctl -u mcu -f
```

실시간으로 흘러갑니다. 멈추려면 `Ctrl+C`.

### 데이터 백업

```bash
sudo bash /opt/mcu/deploy/gcp/backup.sh
```

매일 새벽 4시 자동 백업을 걸려면 `sudo crontab -e` 후 아래 한 줄을 추가합니다.

```
0 4 * * * /bin/bash /opt/mcu/deploy/gcp/backup.sh >> /var/log/mcu-backup.log 2>&1
```

> ⚠️ `sessions.db` 파일을 그냥 복사하면 **깨진 백업**이 됩니다.
> 반드시 이 스크립트를 쓰세요. (안에서 올바른 방법을 씁니다)

클라우드에도 백업본을 올리고 싶으면 [deploy/gcp/backup.sh](deploy/gcp/backup.sh) 맨 위 주석을 보세요.

---

## e2-micro (1GB) — 메모리보다 속도와 전송량

예전에는 메모리가 이 서버의 급소였습니다. 올린 PDF를 통째로 메모리에 올리고, 변환한
그림도 설명이 끝날 때까지 전부 들고 있었거든요. 지금은 **둘 다 디스크에 내려둡니다.**

- 올린 PDF는 받자마자 디스크로 옮깁니다
- 변환한 그림도 디스크에 두고, **보낼 때 한 장씩만** 메모리로 올립니다
- 그래서 요청 하나가 쓰는 메모리가 **올린 파일 크기·그림 장수와 무관해졌습니다**
  (동시에 메모리에 뜨는 그림 수 = 이미지 워커 수)

그래서 업로드 상한을 200MB까지 올렸습니다. 대신 급소가 **속도와 전송량**으로 옮겨갔습니다.

| 대응 | 값 | 이유 |
|---|---|---|
| swap | 2GB | 모자랄 때 죽는 대신 느려지게 (보험) |
| 업로드 상한 | 200MB | 이제 디스크 여유가 기준입니다 |
| 동시 처리 수 | 4개 (원래 16개) | CPU를 나눠 쓰는 서버라 |
| 이미지 상한 | 강의록 100장 / 기출 60장 (코드 기본값 300/200) | 그림 변환이 **한 장씩 차례로** 돌아서 |
| 이미지 워커 | 8개 (코드 기본값 12개) | 공유 vCPU라 늘려도 이득이 적어서 |

그림 처리에서 **변환(렌더)은 한 장씩 차례로** 돌고, 워커 수만큼 병렬로 도는 것은 그 뒤의
LLM 호출뿐입니다. 그래서 상한을 올리면 워커를 같이 올려도 앞부분은 그대로 늘어납니다.

요청 하나가 잠깐 잡는 디스크는 대충 `올린 파일 크기 + 그림 장수 × 3MB`입니다.
상한을 다 채워도 1GB 안쪽이라 기본 부팅 디스크(30GB)면 넉넉합니다.

> **이미지 상한은 전송량에도 걸립니다.** 그림을 게이트웨이로 올리는 만큼 무료 egress
> (월 1GB)가 닳습니다 — 위 [4번 절](#4-돈이-새지-않게--무료-조건-3가지)의 표 참고.

> **위 값은 안전하게 잡은 것이지 한계가 아닙니다.** 실제로 e2-micro에서 코드 기본값
> 100/60/8 로 돌려도 메모리로 죽지 않습니다(이 표의 값이 그 실측선입니다). 아래로 재보고,
> `journalctl` 에 OOM 기록이 없으면 굳이 낮출 이유는 없습니다.

### 지금 값 확인하기

`mcu.env` 에 없는 설정은 **코드 기본값(300/200/12)으로 돕니다.** 서버를 일찍 깔았다면
세 줄이 아예 없을 수 있으니, 파일이 아니라 **실제로 돌고 있는 값**을 보세요:

```bash
sudo cat /proc/$(systemctl show -p MainPID --value mcu)/environ | tr '\0' '\n' | grep IMAGE
```

아무것도 안 나오면 코드 기본값으로 도는 중입니다. 그 값이 마음에 들면 **그대로 파일에
박아두세요** — 나중에 `setup.sh` 를 다시 돌릴 때 빠진 키를 채우는 블록이 100/60/8을
넣어 상한이 소리 없이 반토막 나는 것을 막아줍니다.

```bash
printf 'IMAGE_CAP_LECTURE=300\nIMAGE_CAP_EXAM=200\nIMAGE_WORKERS=12\n' | sudo tee -a /etc/mcu/mcu.env
```

메모리로 죽은 적이 있는지는 아래 "메모리 부족으로 죽은 적이 있는지 보기"로 확인합니다.

**디스크 여유 보기:**

```bash
df -h /
```

### 그림을 더 읽게 하고 싶으면

이미지 상한은 **한쪽 전체** 기준입니다 — 강의록을 5개 올려도 다 합쳐서 상한 한 번입니다
(파일당이 아닙니다). `/etc/mcu/mcu.env` 에서 숫자를 키우면 됩니다.

**메모리 때문에 서버를 키울 필요는 없습니다.** 상한을 올렸을 때 늘어나는 것은 대기 시간과
전송량이지 메모리가 아닙니다. e2-small로 올려서 나아지는 것은 **속도**(그림 변환이 빨라짐)
쪽이니, 느린 게 문제가 아니라면 e2-micro 그대로 상한만 올려도 됩니다.

```bash
sudo nano /etc/mcu/mcu.env
```

`IMAGE_CAP_LECTURE` · `IMAGE_CAP_EXAM` · `IMAGE_WORKERS` 를 고치고 저장한 뒤:

```bash
sudo systemctl restart mcu
```

> 상한에 걸려 못 읽은 쪽이 있으면 앱이 생성 전에 "그림 47쪽 중 30쪽만 반영"이라고
> 물어봅니다. 그 창이 자주 뜨면 상한을 올릴 때입니다.

> **이미 돌아가던 서버라면** — 설치 스크립트는 `mcu.env`가 이미 있으면 시크릿 키를
> 지키려고 파일을 새로 쓰지 않습니다. 그래서 나중에 생긴 설정은 **빠진 것만 뒤에
> 덧붙입니다.** 위 세 줄이 파일에 안 보인다면 지금 코드 기본값(300/200/12)으로 도는
> 중입니다 — 확인은 위 "지금 값 확인하기".
>
> ⚠️ 이때 `setup.sh`를 다시 돌리면 빠진 세 줄이 **e2-micro용 낮은 값(100/60/8)으로**
> 채워져 상한이 소리 없이 반토막 납니다. 지금 값이 마음에 들면 먼저 파일에 박아두세요
> (이미 있는 키는 스크립트가 건드리지 않습니다).

**메모리 상태 보기:**

```bash
free -h
```

**메모리 부족으로 죽은 적이 있는지 보기:**

```bash
sudo journalctl -k --no-pager | grep -i "out of memory"
```

죽어도 몇 초 만에 저절로 되살아납니다. 다만 **그때 처리 중이던 요청 하나는 실패**합니다.

### 그래도 부족하면 → 사양 올리기 (유료, 월 $15~18)

**데이터·설정은 그대로 유지됩니다.** 내 PC에서:

```bash
gcloud compute instances stop mcu-app --zone=us-west1-b
```

```bash
gcloud compute instances set-machine-type mcu-app --zone=us-west1-b --machine-type=e2-small
```

```bash
gcloud compute instances start mcu-app --zone=us-west1-b
```

CPU와 메모리가 넉넉해지므로, 서버의 `/etc/mcu/mcu.env` 에서 `SERVER_THREADS`(→16)와
이미지 상한 `IMAGE_CAP_LECTURE`·`IMAGE_CAP_EXAM`(→300/200)·`IMAGE_WORKERS`(→12)를
올려도 됩니다. 고친 뒤 `sudo systemctl restart mcu`.
(`MAX_UPLOAD_MB` 는 메모리가 아니라 디스크에 걸리므로 e2-micro에서도 이미 200입니다)

> ⚠️ 껐다 켜면 **IP가 바뀝니다.** DuckDNS를 다시 갱신하거나, 6단계에서 토큰을 넣어뒀다면
> 5분 안에 저절로 맞춰집니다.

---

## 게이트웨이가 막혔을 때

배포한 서버에서 문제 생성이 **항상** 실패한다면 게이트웨이 차단을 의심합니다.
서버에서 확인:

```bash
curl -I https://factchat-cloud.mindlogic.ai/
```

응답이 아예 없으면 막힌 것입니다. 이때 선택지는 둘입니다.

**① 다른 LLM 제공사를 쓴다 (서버는 그대로 무료 유지)**

이 앱은 OpenAI·Anthropic·Gemini도 지원합니다([providers/factory.py](providers/factory.py)).
그리고 **API 키는 서버에 저장되지 않고 각자 화면에서 넣습니다.**
즉 쓰는 사람이 자기 키를 넣으면 그대로 동작합니다. 대신 그 제공사는 유료입니다.

**② 서울 리전으로 옮긴다 (월 $18)**

전북대 게이트웨이를 꼭 써야 한다면 무료를 포기하고 한국으로 옮깁니다.
**이 문서의 스크립트는 그대로 쓰고**, 2단계 명령만 아래로 바꿉니다.
3단계부터는 `--zone` 만 `asia-northeast3-a` 로 바꿔서 동일하게 진행합니다.

```bash
gcloud compute instances create mcu-app --project=<프로젝트ID> --zone=asia-northeast3-a --machine-type=e2-small --image-family=ubuntu-2404-lts-amd64 --image-project=ubuntu-os-cloud --boot-disk-type=pd-balanced --boot-disk-size=30GB --tags=http-server,https-server
```

> 같은 돈이면 Oracle Cloud는 서울 리전이 무료입니다 ([배포-오라클.md](배포-오라클.md)).
> 유료로 갈 거라면 그쪽도 비교해보세요.

---

## 문제 해결

| 증상 | 확인할 것 |
|---|---|
| 접속이 안 됨 (계속 로딩) | 3단계 방화벽을 만들었는지, VM에 `http-server` 라벨이 붙었는지 |
| `502 Bad Gateway` | 앱이 죽음 → `sudo journalctl -u mcu -n 50 --no-pager` |
| 자물쇠가 안 붙음 | DuckDNS의 IP가 VM의 현재 IP와 같은지 (`nslookup`) |
| 로그인 시 `redirect_uri_mismatch` | 7-1의 주소를 **https로** 정확히 등록했는지 |
| 문제 생성이 항상 실패 | 위 [게이트웨이가 막혔을 때](#게이트웨이가-막혔을-때) |
| 큰 PDF에서만 끊김 | 메모리 부족 → 위 [e2-micro 메모리](#e2-micro-메모리-1gb--유일하게-조심할-것) |
| 껐다 켠 뒤 접속 안 됨 | IP가 바뀜 → DuckDNS 갱신 |
| 재부팅 후 안 뜸 | `sudo systemctl enable mcu` 확인 |
| 며칠 뒤 요금이 청구됨 | 4절 무료 조건 3가지를 다시 확인 |

### 시크릿 키에 대한 경고

`/etc/mcu/mcu.env` 안의 `FLASK_SECRET_KEY` 를 **절대 바꾸지 마세요.**

이 키로 방문자 구분을 하기 때문에, 바뀌는 순간 **로그인 안 한 사람들이 자기가 만든
세션과 오답노트에 영영 접근할 수 없게 됩니다.**
(설치 스크립트를 다시 돌려도 기존 키는 유지되니 안심하세요)

---

## 비용이 정말 0인지 확인하기

콘솔 → **결제** → **보고서** 에서 봅니다.
무료 조건을 지켰다면 서버·디스크·IP 전부 $0으로 잡힙니다.

가장 흔한 과금 원인은 **부팅 디스크 종류**입니다. 확인하려면 내 PC에서:

```bash
gcloud compute disks describe mcu-app --zone=us-west1-b --format="value(type)"
```

끝이 `pd-standard` 가 아니면 과금 중입니다.
**디스크 종류는 나중에 못 바꿉니다.** 데이터를 백업한 뒤 VM을 지우고 2단계부터 다시 만드세요.

1단계에서 예산 알림을 걸어뒀다면, 뭔가 잘못됐을 때 며칠 안에 메일이 옵니다.
