# 🖥️ 서버 가이드 — 배포된 사이트 다루기

이 앱은 이제 **인터넷에 올라가 있습니다.** 누구나 주소로 접속할 수 있어요.
이 문서는 그 서버에 들어가서 **내가 만든 기능을 실제 사이트에 반영하는 방법**입니다.

> 📘 코드를 GitHub에 올리는 방법은 [팀원_가이드.md](팀원_가이드.md) 를 보세요.
> 이 문서는 **그다음 이야기**입니다.

---

## 🧠 0. 큰 그림 — 지금 뭐가 어디서 돌아가나

컴퓨터가 **세 군데** 있습니다. 이것만 구분되면 나머지는 쉽습니다.

```
  ① 내 노트북              ② GitHub              ③ VM (미국 서버)
  ─────────────            ──────────            ─────────────────
  코드를 쓰는 곳            코드를 모아두는 곳      실제로 24시간 돌아가는 곳
  실행.bat 으로 테스트       (창고)                 팀원·외부인이 접속하는 곳

       │                        │                        │
       │  ── git push ──────►   │                        │
       │                        │  ── update.sh ──────►  │
       │                        │                        │
```

**중요:** GitHub에 올리는 것만으로는 **사이트가 안 바뀝니다.**
서버가 창고에서 코드를 가져오도록 한 번 더 시켜야 합니다. 그게 [4번 항목](#-4-제일-자주-할-일--내-코드를-사이트에-반영하기)입니다.

### 서버 안에서 돌아가는 것들

```
   인터넷에서 접속
        │
        ▼
   Caddy         ← 문지기. https 자물쇠를 자동으로 관리
        │
        ▼
   앱 (serve.py)  ← 우리 파이썬 코드. mcu 라는 이름의 서비스로 돌아감
        │
        ├──► sessions.db      데이터(세션·오답노트·회원)가 쌓이는 곳
        └──► LLM 게이트웨이     문제 만들 때 외부로 요청
```

---

## 🛠️ 1. 최초 1회 준비 (딱 한 번만)

### ① 프로젝트에 초대받기 ← 이게 제일 먼저

**서버 주인(프로젝트 관리자)에게 요청해야 합니다.** 이게 없으면 아래를 다 해도 접속이 안 됩니다.

관리자에게 이렇게 말하세요:

> "GCP 프로젝트에 제 Google 계정 `내이메일@gmail.com` 좀 추가해주세요.
> 역할은 **Compute 인스턴스 관리자(v1)** 로요.
> 그리고 **프로젝트 ID** 도 알려주세요."

**프로젝트 ID** 는 `medical-web-000000` 같은 문자열입니다. 아래 ④에서 씁니다.
공개 저장소라 문서에 적어두지 않았으니, **팀 채팅에서 받아 두세요.**

<details>
<summary>관리자가 할 일 (관리자만 보세요)</summary>

Google Cloud 콘솔 → 왼쪽 메뉴 → **IAM 및 관리자** → **IAM** → 위쪽 **액세스 권한 부여**

- 새 주 구성원: 팀원 Google 이메일
- 역할: **Compute 인스턴스 관리자(v1)**
- 저장

SSH가 그래도 안 된다고 하면 **서비스 계정 사용자** 역할도 추가로 주세요.

</details>

### ② gcloud 설치

Google Cloud를 명령어로 다루는 프로그램입니다.

<https://cloud.google.com/sdk/docs/install> → Windows 64-bit 설치 파일 → 실행 → 기본값 그대로 다음

**설치가 끝나면 PowerShell 창을 닫고 새로 여세요.** (안 그러면 명령을 못 찾습니다)

### ③ 로그인

```bash
gcloud auth login
```

브라우저가 열리면 **①에서 초대받은 그 Google 계정**으로 로그인하고 허용을 누릅니다.

### ④ 프로젝트 지정 (한 번만)

①에서 받은 프로젝트 ID를 기본값으로 등록해둡니다. **한 번만 하면 이후 모든 명령이 짧아집니다.**

```bash
gcloud config set project 여기에_프로젝트_ID
```

> ⚠️ `여기에_프로젝트_ID` 자리에 실제 값을 넣으세요.
> **꺾쇠(`< >`)를 같이 넣으면 안 됩니다.** 그대로 전달돼서 "그런 프로젝트 없음" 오류가 납니다.

### ⑤ 잘 됐는지 확인

```bash
gcloud compute instances list
```

`mcu-app` 이 `RUNNING` 으로 보이면 준비 끝입니다.

> ❌ **권한 오류가 나면** ①이 안 된 것입니다. 관리자에게 다시 요청하세요.
> ❌ **`gcloud` 를 못 찾으면** ②를 하고 PowerShell을 새로 열지 않은 것입니다.

---

## 🚪 2. 서버에 들어가기

### 들어가는 명령

**내 PC의 PowerShell** 에서:

```bash
gcloud compute ssh mcu-app --zone=us-west1-b
```

처음이면 SSH 키를 만들지 물어봅니다 → `Y`
암호(passphrase)를 물어보면 → **그냥 Enter 두 번** (비워도 됩니다)

### 들어갔는지 확인하는 법

**명령 입력줄 왼쪽(프롬프트)** 을 보세요. 이게 제일 헷갈리는 부분입니다.

| 보이는 것 | 여기는 |
|---|---|
| `PS C:\Users\...>` | **내 노트북** |
| `내이름@mcu-app:~$` | **서버 안** ✅ |

헷갈리면 이걸 쳐보세요. `mcu-app` 이 나오면 서버 안입니다.

```bash
hostname
```

### 나오는 법

```bash
exit
```

또는 `Ctrl+D`. 나와도 **서버는 계속 돌아갑니다.** 사이트가 꺼지는 게 아니에요.

### 위 명령이 잘 안 될 때 — 브라우저로 접속

이 방법이 더 쉬울 수도 있습니다.

<https://console.cloud.google.com> → 왼쪽 메뉴 → **Compute Engine** → **VM 인스턴스**
→ `mcu-app` 줄 오른쪽의 **SSH** 버튼 클릭 → 검은 창이 새로 열립니다. 거기가 서버 안입니다.

### 복사·붙여넣기가 안 될 때

터미널에서는 **`Ctrl+C` 가 복사가 아니라 "실행 중인 명령 강제 중단"** 입니다. 그래서 다른 키를 씁니다.

| 하려는 것 | 누를 키 |
|---|---|
| 붙여넣기 | **`Ctrl+Shift+V`** — 안 되면 **마우스 오른쪽 클릭** |
| 복사 | 드래그로 선택 → **`Ctrl+Shift+C`** |

오른쪽 클릭이 가장 확실합니다.

---

## 📁 3. 서버 안에 뭐가 어디 있나

일부러 **코드 · 데이터 · 설정을 세 곳으로 나눠**뒀습니다. 코드를 갈아엎어도 데이터가 안 날아가게요.

| 경로 | 뭐가 있나 | 건드려도 되나 |
|---|---|---|
| `/opt/mcu` | **우리 코드** (GitHub에서 받아온 것) | ⚠️ 직접 고치지 마세요 — update.sh가 덮어씁니다 |
| `/var/lib/mcu/sessions.db` | **데이터** (세션·오답노트·회원) | 🚫 절대 지우지 마세요 |
| `/var/lib/mcu/backups/` | 자동 백업본 (14일치) | ✅ 봐도 됩니다 |
| `/etc/mcu/mcu.env` | **설정·비밀 키** | ⚠️ 아래 7번 경고 필독 |
| `/etc/caddy/Caddyfile` | https 문지기 설정 | ⚠️ 건드릴 일 거의 없음 |
| `/var/log/caddy/mcu.log` | 접속 기록 | ✅ 봐도 됩니다 |

**앱은 `mcu` 라는 이름의 서비스로 돌아갑니다.** 아래 명령들에 계속 나오는 그 `mcu` 입니다.

---

## 🔄 4. 제일 자주 할 일 — 내 코드를 사이트에 반영하기

**전제: 내 코드가 이미 GitHub의 `main` 에 병합돼 있어야 합니다.**
(PR이 merge된 상태. [팀원_가이드.md](팀원_가이드.md) 7단계까지 끝난 것)

서버에 접속한 상태에서 **이 한 줄이면 끝입니다.**

```bash
sudo bash /opt/mcu/deploy/gcp/update.sh
```

### 이게 하는 일 / 안 하는 일

| 한다 ✅ | 안 한다 ❌ |
|---|---|
| GitHub `main` 에서 최신 코드 받기 | **데이터베이스 건드리기** |
| 새 패키지 설치 (`requirements.txt`) | **설정 파일 건드리기** (`mcu.env`) |
| 앱 재시작 | |

설정과 데이터를 안 건드리는 건 **일부러 그렇게 만든 것**입니다. 비밀 키가 날아가면 큰일이거든요.

### 끝나면 확인

명령이 끝나면 화면에 `active (running)` 이 보입니다. 그다음 **브라우저에서 사이트를 새로고침**해서 내 기능이 반영됐는지 보세요.

### `update.sh` 만으로 안 되는 경우

| 상황 | 추가로 할 일 |
|---|---|
| **DB 컬럼을 추가**했다 | 없음. 앱이 켜질 때 자동 반영됩니다 |
| **DB 컬럼을 삭제·이름 변경**했다 | 자동 안 됨. 팀에 먼저 알리고 같이 처리하세요 |
| **새 환경변수**를 쓰는 코드다 | `/etc/mcu/mcu.env` 에 직접 추가 → 아래 6번 재시작 |
| **접속 주소가 바뀌었다** | Google 콘솔의 로그인 리디렉션 URI도 수정 |

---

## 🔍 5. 사이트가 이상할 때 — 들여다보기

### 앱이 살아 있나

```bash
sudo systemctl status mcu
```

초록색 **`active (running)`** 이면 정상입니다. 빨간 `failed` 면 아래 로그를 보세요.
(보고 나서 빠져나오려면 `q`)

### 로그 보기 — 원인은 거의 여기 있습니다

최근 50줄:

```bash
sudo journalctl -u mcu -n 50 --no-pager
```

실시간으로 흘려보기 (멈추려면 `Ctrl+C`):

```bash
sudo journalctl -u mcu -f
```

### 메모리 확인

이 서버는 **메모리가 1GB뿐**이라 큰 PDF를 처리할 때 빠듯합니다.

```bash
free -h
```

메모리 부족으로 앱이 죽은 적이 있는지:

```bash
sudo journalctl -k --no-pager | grep -i "out of memory"
```

> 죽어도 **몇 초 만에 저절로 되살아납니다.** 다만 그때 처리 중이던 요청 하나는 실패합니다.

### 디스크 확인

```bash
df -h /
```

---

## 🔧 6. 재시작하기

설정을 바꿨거나 뭔가 이상할 때:

```bash
sudo systemctl restart mcu
```

몇 초면 됩니다. **데이터는 그대로입니다.**

서버 자체를 재부팅해야 한다면:

```bash
sudo reboot
```

접속이 끊깁니다. 2분쯤 뒤 다시 들어가면 **앱은 저절로 켜져 있습니다.**

---

## 🚫 7. 절대 하면 안 되는 것

### ① `FLASK_SECRET_KEY` 를 바꾸지 마세요

`/etc/mcu/mcu.env` 안에 있습니다. 이 키로 방문자를 구분하기 때문에,
**바뀌는 순간 로그인 안 한 사람들이 자기가 만든 세션·오답노트에 영영 접근할 수 없게 됩니다.**

### ② `/var/lib/mcu` 를 지우지 마세요

여기가 **모든 데이터**입니다. 지우면 복구할 수 없습니다.

### ③ `/opt/mcu` 안의 파일을 직접 고치지 마세요

고쳐도 `update.sh` 를 돌리면 덮어써집니다. **코드 수정은 항상 내 노트북 → GitHub → update.sh 순서로.**

### ④ VM을 정지(stop)하지 마세요

껐다 켜면 **인터넷 주소(IP)가 바뀌어서** 사이트에 접속이 안 되게 됩니다.
정말 필요하면 서버 주인에게 먼저 말하세요.

### ⑤ `sudo rm -rf` 는 쓰지 마세요

리눅스에서 실수로 시스템을 통째로 날리는 대표적인 명령입니다.

---

## 📋 8. 자주 쓰는 명령어 치트시트

**내 PC에서:**

```bash
gcloud compute ssh mcu-app --zone=us-west1-b
```

**서버 안에서:**

| 하려는 것 | 명령 |
|---|---|
| 코드 반영 | `sudo bash /opt/mcu/deploy/gcp/update.sh` |
| 상태 확인 | `sudo systemctl status mcu` |
| 로그 보기 | `sudo journalctl -u mcu -n 50 --no-pager` |
| 재시작 | `sudo systemctl restart mcu` |
| 메모리 확인 | `free -h` |
| 지금 어디인지 | `hostname` |
| 나가기 | `exit` |

---

## 🆘 9. 막혔을 때

| 증상 | 확인할 것 |
|---|---|
| `gcloud` 명령을 못 찾음 | 1-② 설치 후 PowerShell을 새로 열었는지 |
| SSH에서 권한 오류 | 1-① 프로젝트 초대를 받았는지 |
| `command not found` | 프롬프트를 보세요. 서버 밖에서 리눅스 명령을 친 것입니다 |
| `-fsSL` 매개변수 오류 | 같은 이유 — 내 PC에서 리눅스 명령을 친 것입니다 |
| 사이트가 안 열림 | `sudo systemctl status mcu` → `failed` 면 로그 확인 |
| `502 Bad Gateway` | 앱이 죽음 → `sudo journalctl -u mcu -n 50 --no-pager` |
| 코드를 올렸는데 사이트가 그대로 | ① PR이 main에 merge됐는지 ② `update.sh` 를 돌렸는지 |
| 문제 생성만 실패 | LLM 게이트웨이 문제일 수 있음 → [배포-GCP.md](배포-GCP.md) 참고 |
| 큰 PDF에서만 끊김 | 메모리 부족 → 5번의 메모리 확인 |

**그래도 모르겠으면** 아래 두 개를 복사해서 팀 채팅에 올리세요. 그거면 대부분 원인이 잡힙니다.

```bash
sudo systemctl status mcu
```

```bash
sudo journalctl -u mcu -n 50 --no-pager
```

---

## ✅ 한 줄 요약

> **내 노트북에서 코드 → GitHub에 PR → main에 merge → 서버에서 `update.sh` → 사이트 반영**

서버를 만드는 방법 자체가 궁금하면 [배포-GCP.md](배포-GCP.md) 를 보세요.
